Набросок:

```
[1] Чтение producers_list.txt
 ↓
[2] Поиск сайта и парсинг информации
 ↓
[3] Обогащение LLM:
     — описание (строгое, по сайту)
     — тегирование
     — комментарий Вовы (отдельно!)
 ↓
[4] Формирование markdown-файла
     → Сохраняется в sort/вова/готовое
 ↓
[5] Ручная проверка:
     — вставка картинки и иконки
     — перенос в /project/data/vst/
 ↓
[6] (авто/полуавто) Создание поста в Telegram
     — Превью из файла (название, краткое описание)
     — Комментарий Вовы
```
