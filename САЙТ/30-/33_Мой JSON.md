
## 📦 Что такое JSON?

**JSON (JavaScript Object Notation)** — это простой формат хранения и обмена данными. Он представляет собой **структуру из "ключей" и "значений"**, которые можно воспринимать как таблицу с названием поля и его содержимым.

🧩 Базовая структура:

```
{
  "ключ1": "значение1",
  "ключ2": 123,
  "ключ3": true,
  "ключ4": ["список", "значений"],
  "ключ5": {
    "вложенный_ключ": "вложенное_значение"
  }
}
```

## ❓ Какие слова перед двоеточием ("ключи") допустимы?

### 🔹 1. **Если файл используется только для твоего проекта**

Ты можешь использовать **любые ключи**, главное — **следи за читаемостью и логикой**. Например:

```
{
  "имя": "Вова",
  "роль": "Парсер VST",
  "стиль_общения": "ироничный",
  "парсинг_область": ["vst", "плагины", "аудиоинструменты"]
}
```

### 🔹 2. **Если ты хочешь, чтобы LLM или другие программы правильно понимали структуру**

Тогда стоит использовать **общепринятые ключи**:

- `id`, `name`, `description`, `functions`, `skills`, `style`, `intent`, `input_type`, `output_format`, `communication_style`, `personality`, `triggers`, `avoid_topics`, `permissions`, `data_sources` и др.

Пример:

```
{
  "id": "vova",
  "name": "Вова",
  "role": "Парсер и обозреватель аудиоплагинов",
  "functions": ["Парсинг данных", "Форматирование в Markdown", "Публикация в Telegram"],
  "communication_style": {
    "public": "ироничный, дружелюбный, на грани шутки и критики",
    "internal": "технарь с ноткой занудства"
  }
}
```

Такие ключи помогают не только людям, но и нейросетям (в будущем) понять, кто такой Вова и как с ним работать.

## 🚫 Ошибки, которых стоит избегать:

|Ошибка|Пример|Правильно|
|---|---|---|
|Ключ без кавычек|`id: "vova"`|`"id": "vova"`|
|Пропущена запятая|`"id": "vova" "name": "Вова"`|`"id": "vova", "name": "Вова"`|
|Последняя запятая|`"name": "Вова",`|(без запятой, если больше нет полей)|
|Невалидный символ|`"имя": Вова`|`"имя": "Вова"`|

## 🧠 Заключение

- **Перед двоеточием — всегда ключ в кавычках.**
    
- Ключ может быть **любым**, но лучше делать осмысленным и однозначным.
    
- Если планируешь взаимодействие с LLM, выбирай **универсальные или предсказуемые названия ключей**.
    
- Структура может быть **вложенной** — внутри ключа может быть ещё один JSON-объект или массив (`[]`).

## 📚 Базовые понятия

### 🔹 JSON-объект (`{ ... }`)

Это **набор пар "ключ: значение"**, заключённый в фигурные скобки.  
Каждое значение можно получить по имени ключа.

Пример:

```
{
  "имя": "Вова",
  "роль": "Парсер VST"
}
```

### 🔸 JSON-массив (`[ ... ]`)

Это **упорядоченный список значений**, заключённый в квадратные скобки.  
В нём **нет ключей**, только значения, как в списке.

Пример:

```
["VST", "AU", "AAX"]
```

## 🔁 Вложенность

Ты можешь **внутри объекта** положить **массив**:

```
{
  "поддерживаемые_форматы": ["VST", "AU", "AAX"]
}
```

Или внутри массива — объекты:

```
{
  "источники": [
    {
      "название": "Arturia",
      "сайт": "https://www.arturia.com"
    },
    {
      "название": "Native Instruments",
      "сайт": "https://www.native-instruments.com"
    }
  ]
}
```

## 📦 Пример из жизни

Вот так может выглядеть часть файла `vova.json`:

```
{
  "id": "vova",
  "name": "Вова",
  "role": "Парсер VST",
  "target_companies": ["Arturia", "u-he", "Spitfire Audio"],
  "functions": [
    "Сканирует сайты производителей",
    "Собирает описание, ссылки, форматы",
    "Публикует в Telegram"
  ],
  "communication_style": {
    "public": "шутит, язвит, но по делу",
    "with_hugo": "завуалированный троллинг",
    "with_petrova": "притворяется прилежным"
  }
}
```

## ✍️ Шпаргалка:

| Сущность              | Вид                      | Описание                                        |
| --------------------- | ------------------------ | ----------------------------------------------- |
| `{ ... }`             | Объект                   | Словарь с парами ключ:значение                  |
| `[ ... ]`             | Массив                   | Просто список значений (строк, чисел, объектов) |
| `{ "ключ": [ ... ] }` | Объект с массивом        | Часто используется                              |
| `{ "ключ": { ... } }` | Объект с другим объектом | Для вложенности                                 |
## 🤖 Как LLM "понимает" значения ключей?

LLM **не ограничена строгим перечнем ключей**. Она **не требует "официальных" ключей**, как, например, API-интерфейсы. Но она использует контекст и накопленный опыт — то есть:

### 1. **Модель обучалась на миллиардах JSON-файлов**

И она знает, что:

- `name` часто = имя
    
- `description` = описание
    
- `role` = роль
    
- `functions`, `skills`, `goals` = задачи
    
- `communication_style`, `tone`, `behavior` = манера общения

Поэтому если ты пишешь:

```
"communication_style": {
  "public": "дружелюбный",
  "internal": "официальный"
}
```

LLM по контексту _угадает_, что `"public"` означает тон общения с публикой.

### 2. **Модель ориентируется на _структуру и соседние ключи_**

Если рядом есть:

- `"communication_style"`, `"tone"`, `"audience"`, `"voice"`,  
    то `"public"` = "тон публичного общения".


Если же файл такой:

```
"visibility": "public"
```

То `"public"` означает — "открыто для всех".

👉 То есть всё зависит от контекста ключей.

### 3. **Ты можешь помочь ей понять точнее**

Если хочешь избежать неоднозначности, можно писать:

```
"communication_style": {
  "tone_public": "дружелюбный, открытый",
  "tone_with_team": "технический и краткий",
  "tone_with_hugo": "троллинг"
}
```

Или даже вот так:

```
"tone": {
  "public_audience": "дружелюбный",
  "internal_use": "сухой и точный",
  "toward_hugo": "саркастичный"
}
```

## 🧩 Общепринятые ключи

Нет строгого стандарта, но вот список **ключей, которые LLM точно распознаёт по смыслу**:

|Ключ|Описание|
|---|---|
|`id`|Уникальный идентификатор|
|`name`|Имя персонажа или агента|
|`role`|Роль в системе или проекте|
|`description`|Описание|
|`functions`|Основные функции или задачи|
|`skills`|Навыки|
|`goals`|Цели|
|`communication_style`, `tone`, `voice`|Стиль общения|
|`personality_traits`|Черты характера|
|`data_sources`|Источники данных|
|`output_format`|Формат вывода|
|`access_level`|Уровень доступа|
|`trigger_words`|Слова, на которые реагирует|
|`avoid_topics`|Темы, которых избегает|
|`permissions`|Что разрешено|
|`input_format`|Что принимает на вход|
|`output_channel`|Куда публикует|

## 🧠 Вывод

- Ключи можно называть как хочешь, **если контекст очевиден**.
    
- Для LLM достаточно, чтобы названия были **осмысленными**.
    
- Лучше использовать **интуитивные, привычные слова**, чтобы LLM точно не запуталась.
    
- Если сомневаешься — просто сделай немного более "подсказочный" стиль ключей.

## 🔧 В JSON важны отступы?

### 📌 **Ответ: отступы важны для читаемости, но не обязательны для работы.**

JSON **не требует отступов или пробелов**, чтобы файл был **валидным** (то есть правильно прочитан программой).

Это сработает:

```
{"name":"Вова","role":"Парсер"}
```

И это сработает:

```
{
  "name": "Вова",
  "role": "Парсер"
}
```

## 🧠 Но! Зачем тогда делают отступы?

### ✅ Для **человека**:

- Видно, что где вложено,
    
- Легче ориентироваться,
    
- Проще редактировать и не запутаться.

### ✅ Для **публики**:

- Это часть "прозрачности", особенно если ты выкладываешь JSON в `.md` или GitHub — читаемый формат = уважаемый формат.

## 📏 Сколько пробелов?

Тут есть негласные стандарты:

|Стиль|Пример|Когда использовать|
|---|---|---|
|`2 пробела`|👶|Часто в веб-разработке|
|`4 пробела`|👨‍🔧|Стандарт в Python-среде, читабельно|
|`табуляция`|😼|Иногда в редакторах, но не любим Git'ом|

📌 Самое популярное: **4 пробела**.

## 📦 Пример «хорошего» JSON

```
{
    "id": "vova",
    "name": "Вова",
    "role": "Парсер VST",
    "functions": [
        "Парсинг сайтов производителей",
        "Структурирование данных",
        "Публикация в Telegram"
    ],
    "communication_style": {
        "public": "дружелюбный, с нотками иронии",
        "internal": "сухой и точный"
    }
}
```

## ✅ Резюме:

| Важно для компьютера? | ❌ Нет |  
| Важно для тебя, других людей и LLM? | ✅ Да |  
| Рекомендуемый стиль? | 2 или 4 пробела отступа для вложенных объектов/массивов |  
| Лучше табы или пробелы? | Лучше **пробелы** (чаще 4) |

---
#JSON
