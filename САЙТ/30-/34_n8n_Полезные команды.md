
| Действие                            | Команда           |
| ----------------------------------- | ----------------- |
| Перезапустить n8n                   | `pm2 restart n8n` |
| Остановить n8n                      | `pm2 stop n8n`    |
| Удалить n8n из pm2                  | `pm2 delete n8n`  |
| Просмотреть логи                    | `pm2 logs n8n`    |
| Перезаписать автозагрузку (на всяк) | `pm2 save`        |
## 📌 Но запускается ли workflow сам?

Вот тут есть нюанс:

|Тип workflow|Работает сам?|Условия запуска|
|---|---|---|
|**Webhook**|❌|Пока не придёт входящий запрос|
|**Cron (Time Trigger)**|✅|Срабатывает по расписанию|
|**Manual (ручной)**|❌|Работает только из UI|
|**Trigger (Telegram, Discord и пр.)**|✅|Работает при получении события|

То есть, **автоматизация зависит от типа триггера**. `n8n` сам по себе работает всегда, но **цепочки** (workflow) запускаются, когда есть триггер.