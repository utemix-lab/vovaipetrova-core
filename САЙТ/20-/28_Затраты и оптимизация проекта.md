## 📦 ЧАСТЬ 1. Объём хранения: сколько чего влезет в 1000 ГБ?

### 📌 Расчёт базируется на следующих оценках (в среднем на 1 модель):

|Компонент|Средний размер|1000 моделей|10 000 моделей|40 000 моделей|
|---|---|---|---|---|
|`.md` страница|20–50 КБ|~30 МБ|~300 МБ|~1.2 ГБ|
|Скриншоты|~300–800 КБ|~500 МБ|~5 ГБ|~20 ГБ|
|JSON enriched|50–100 КБ|~80 МБ|~800 МБ|~3.2 ГБ|
|**Общий вес на 1 модель**|~600–1000 КБ|~1 ГБ|~10 ГБ|~40 ГБ|

---

## 🔍 А теперь **в 1000 ГБ уместится:**

|Формат хранения|Примерное количество моделей|
|---|---|
|Только `.md`|10 000 000+ (!)|
|`.md` + скриншоты|~1 000 000|
|`.md` + скриншоты + enriched JSON|~500 000|
|`.md` + скриншоты + enriched JSON + копии из других источников|~100 000–200 000|

⚠️ Реально ты упрёшься раньше — не в место, а в **структуру, индексацию, кеши, поиски и генерацию страниц сайта.** Поэтому:

---

## 🧠 ЧАСТЬ 2. Нужно ли хранить enriched JSON **после формирования `.md`?**

**Кратко: нет, но... стоит подумать.**

### 📌 JSON нужен:

- Пока ты **автоматизируешь формирование `.md`**
    
- Пока база **обновляется/обогащается**
    
- Для **повторного парсинга**, если API Hugging Face изменится
    

### 📌 Когда можно удалять JSON:

- Когда `.md`-страница **сформирована и не планируется к обновлению**
    
- Когда ты **экспортировал данные в сайт**
    

📦 **Лучшее решение:**

- Делать _архивную папку `json-archive/`_ с уже использованными `.json`
    
- Сжать её (`.tar.gz`) раз в неделю
    
- И удалить по мере уверенности
    

---

## 📦 ЧАСТЬ 3. Стратегия хранения

### 🔹 Хранить обязательно:

- `.md` (основной контент)
    
- Скриншоты (визуальное сопровождение)
    
- Изображения/логотипы
    
- Внешние ссылки (в виде ссылок, не файлов)
    

### 🔸 Хранить временно:

- enriched JSON
    
- необработанные `.txt`-дампы
    
- кэши API и логов
    

---

## 🌐 ЧАСТЬ 4. Связь с сайтом

Ты абсолютно прав — **каждая `.md`-страница потом будет превращена в HTML-страницу сайта**.  
Возможные подходы:

|Подход|Преимущества|Недостатки|
|---|---|---|
|Статика из `.md` (Hugo / Eleventy / Jekyll)|Быстро, просто, надежно|Нет интерактива|
|Генерация через CMS (WordPress + Obsidian-интеграция / API)|Гибко, визуально|Сложнее настроить структуру|
|Live-рендер `.md` на сервере (например, через Obsidian Publish-стиль)|Единый источник истины|Требует серверной обработки|

В любом случае `.md` — идеальный исходник:

- читабелен вручную,
    
- пригоден для LLM,
    
- хорошо индексируется.
    

---

## 🧠 ИТОГИ

| Вопрос                             | Ответ                                                      |
| ---------------------------------- | ---------------------------------------------------------- |
| Хватит ли 1000 ГБ?                 | Для **100 000–500 000 моделей** — **да**                   |
| Нужно ли хранить `.json` навсегда? | ❌ Необязательно — можно архивировать и удалять             |
| Как не множить сущности?           | Сохранять только `.md`, изображения, метаинфу              |
| Можно ли строить сайт на `.md`?    | ✅ Да, и это лучший формат — как исходник, так и веб-основа |
## ✅ Перенос данных с одного диска на другой — **не сложно**, особенно если **изначально заложить минимальную гибкость**.

---

### 📦 Что на самом деле нужно будет перенести:

- Папки `output/`, `screenshots/`, `markdown/`, `json/` — то есть **только данные**
    
- **Скрипты, n8n, systemd, Docker и т.д.** — можно **не трогать**, они могут оставаться на основном диске
    

---

## 🔄 Перенос HDD → SSD: варианты

|Вариант|Сложность|Комментарий|
|---|---|---|
|`rsync -a /mnt/old /mnt/new`|🟢 Легко|Перенос всех файлов с сохранением прав|
|Смена симлинка|🟢 Легко|Если `/home/Hugo/output` — это `ln -s`, просто перепривязать|
|Перенос + смена `fstab`|🟡 Средне|Только если диск монтировался напрямую|

---

## ✅ Как подготовиться сейчас (и не пожалеть потом)

1. **Сделай всю структуру хранения внешней по отношению к коду**  
    Например, данные пишем в `/mnt/hugo_data/output`,  
    а `output/` в `/home/Hugo` — это просто **символическая ссылка**:
    
    bash
    
    КопироватьРедактировать
    
    `ln -s /mnt/hugo_data/output /home/Hugo/output`
    
2. **Не хардкодить абсолютные пути в скриптах**  
    Вместо `/home/Hugo/output/markdown/model.md` используй:
    
    python
    
    КопироватьРедактировать
    
    `from pathlib import Path BASE = Path(__file__).parent OUTPUT = BASE / "output"`
    
    Или использовать `.env` или `config.json`:
    
    json
    
    КопироватьРедактировать
    
    `{   "output_dir": "/mnt/hugo_data/output" }`
    
3. **n8n:**  
    Пути к файлам или командам в workflow лучше брать из переменных или `env`, тогда при переезде **достаточно изменить одну переменную**.
    

---

## 🧠 Что делать при переносе на SSD позже?

1. Остановить Hugo / n8n (если они активно пишут)
    
2. `rsync` все данные:
    
    bash
    
    КопироватьРедактировать
    
    `rsync -a /mnt/hdd_hugo_data/ /mnt/ssd_hugo_data/`
    
3. Обновить симлинк:
    
    bash
    
    КопироватьРедактировать
    
    `rm /home/Hugo/output ln -s /mnt/ssd_hugo_data/output /home/Hugo/output`
    
4. Готово!
    

---

## ✅ Вывод

|Вопрос|Ответ|
|---|---|
|Трудно ли будет перенести данные?|❌ Нет, если использовать `rsync` и `ln -s`|
|Нужно ли сейчас учитывать это в скриптах?|✅ Да, но **минимально** — просто не хардкодить пути|
|Лучше использовать симлинк?|✅ Абсолютно! Это самое гибкое решение|