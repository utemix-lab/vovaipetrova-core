## 📦 Что делает:

- Заглядывает на внешний сайт (например, `https://hunyuan3d.github.io/`)
    
- Вытягивает:
    
    - 🏷 `title`, `description`, `excerpt`
        
    - 🖼 До 5 изображений (`<img>`)
        
- Обновляет `enriched_hunyuan3d.json`
    
- Добавляет в `.md` новый раздел `## 🌐 Внешний сайт`

## 📁 Что изменится:

- Файл `enriched_hunyuan3d.json` обновится
    
- В `output/enriched_hunyuan3d.md` появится:

```
## 🌐 Внешний сайт: https://hunyuan3d.github.io/
**Заголовок:** Hunyuan3D — high-res 3D generation
**Описание:** Интерактивный генератор 3D из изображения или текста.
**Фрагмент текста:** ...
![preview](./assets/hunyuan3d/site_image_1.jpg)
```

1. **Основной модуль Hugo** должен всё это делать — не как отдельные скрипты, а как **единая цепочка**:
    
    - вытаскивать внешние ссылки из `.md`,
        
    - делать скриншоты сайтов (а не HuggingFace),
        
    - сохранять их с **именем модели** (например `Hunyuan3D.png`),
        
    - сохранять `.md` с тегами, описанием и блоком ролевого текста Хьюго (в будущем через LLM).
        
2. Текущий эксперимент — **временный standalone-скрипт** только для скриншота, чтобы:
    
    - убедиться, что работает headless-режим Playwright,
        
    - проверить вывод (полноразмерный или нет),
        
    - продумать переименование, директории хранения и вызов из основного скрипта Hugo.

### ✅ Что нужно добавить:

- Скриншот по **внешней ссылке** из `external_links`.
    
- Переименование скриншота в `ИмяМодели.png`.
    
- Перенос в директорию `/mnt/hugo_data/screenshots/` или подобную.
    
- Обогащение `.md` с:
    
    - извлечённым текстом из страницы HF (например, warnings),
        
    - LLM-описанием (позже),
        
    - автоопределёнными тегами (например, "3d", "text-to-3d", "tencent").