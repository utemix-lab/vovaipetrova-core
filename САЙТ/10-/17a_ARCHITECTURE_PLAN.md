
# 🏗 Проект: Инфраструктура сайта и среды разработки

## 📚 Общая архитектура

- Ubuntu VPS (Selectel)
  - Операционная система: Ubuntu 24.04 LTS
  - Используется для:
    - Хостинга сайтов (Nginx / Apache)
    - Работы backend (PHP / Python / Node)
    - MySQL / PostgreSQL
    - Docker для изолированных сервисов (по мере роста)
  - Без GUI. Управление только через SSH / SFTP.

- Windows Server VPS (Selectel)
  - Операционная система: Windows Server 2019 / 2022 Standard
  - Используется для:
    - Удалённой разработки (VS Code, Git, Obsidian)
    - RDP-доступа для работы с браузерами и файлами
    - Подключения к Ubuntu (SSH, WinSCP, Putty)
  - По сути «чистая лаборатория» без локальных багов Windows.

---

## 🔐 Сеть и безопасность

- У каждого VPS свой публичный IP.
- Security Groups / Firewall:
  - Ubuntu:
    - разрешить: `22 (SSH)`, `80 (HTTP)`, `443 (HTTPS)`
    - всё остальное закрыто
  - Windows:
    - разрешить: `3389 (RDP)` (по возможности только с твоего IP)
- Использовать SSH-ключи для входа на Ubuntu, пароли отключить.

---

## ⚙ ПО и окружение

### На Ubuntu
- Nginx (или Apache)
- Let’s Encrypt (SSL)
- PHP / Python (по проекту)
- MySQL / PostgreSQL
- Docker + docker-compose (по мере надобности)
- fail2ban + ufw
- Регулярные бэкапы (snapshots + dumps).

### На Windows Server
- VS Code
- Git
- Docker Desktop (по необходимости для сборки образов)
- Putty / MobaXterm / WinSCP для SSH + SFTP
- Obsidian для базы знаний
- Chrome / Firefox для дебага

---

## 🚀 CI/CD
- На старте:
  - ручной деплой через SSH или SFTP (git pull, rsync)
- На следующем этапе:
  - автоматизация через GitHub Actions / GitLab CI

---

## 💾 Хранение данных и безопасности
- SSH-ключи хранятся в `~/.ssh` на Ubuntu и в Windows Server.
- Репозитории и конфиги в приватных Git.
- Конфиги Docker, .env-файлы, доступы к БД хранятся локально и в менеджере паролей.

---

## 📝 Документация
- Архитектурная схема (PlantUML / mermaid).
- README.md с командами деплоя и восстановления.
- Список всех установленных пакетов и их версий.

---

## ⏱ Дорожная карта (Roadmap)
| Этап                                | Статус    |
|--------------------------------------|-----------|
| Создание Ubuntu VPS                  | ✅ done   |
| Настройка Nginx / SSH / firewall     | ⏳ в работе |
| Создание Windows VPS                 | 🚀 next   |
| Установка VS Code, Git, SSH на Win   | ❌ todo   |
| Первая загрузка сайта / бэкапы       | ❌ todo   |
| Автоматизация (CI/CD)                 | 🚀 future |
| Документация и база знаний           | ⏳ в процессе |

---

> 🧠 **Примечание:**  
> Этот документ — как «карта проекта», она показывает общую логику.  
> Реализацию будем делать пошагово, чтобы твои нейроны всё это надёжно впитали.  
> Обновляй этот файл после каждого шага, так ты создашь свою живую историю инфраструктуры.



```mermaid
flowchart TD
    subgraph UVPS[Ubuntu VPS]
        direction TB
        A1[Nginx / Apache]
        A2[PHP / Python / Node]
        A3[MySQL / PostgreSQL]
        A4[Docker - по мере роста]
    end

    subgraph WS[Windows Server]
        direction TB
        B1[VS Code, Git, Obsidian]
        B2[Putty, WinSCP]
        B3[Chrome, Firefox]
    end

    subgraph LPC[Local PC]
        direction TB
        C1[mstsc - RDP клиент]
        C2[Локальные файлы, бэкапы]
    end

    C1 --> WS
    WS -->|SSH/SFTP| UVPS
    C2 -->|RDP| WS

