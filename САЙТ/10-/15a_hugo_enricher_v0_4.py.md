
## 🧠 Цель `hugo_enricher_v0_4.py`

- Обработать входные данные о модели с Hugging Face (JSON).
    
- Проверить внешние ссылки (если есть).
    
- Извлечь ключевую информацию с сайта модели.
    
- Обогатить данные (добавить заголовок, описание, возможно изображения).
    
- Подготовить данные к публикации в Telegram или в базу.
   
## 🧱 **Структура скрипта**

```
# hugo_enricher_v0_4.py

import json
import requests
from bs4 import BeautifulSoup
from urllib.parse import urlparse
import logging
import sys

# ——————————————————————————————
# 1. Настройка логирования
# ——————————————————————————————
logging.basicConfig(level=logging.INFO, format='[%(levelname)s] %(message)s')


# ——————————————————————————————
# 2. Загрузка входных данных
# ——————————————————————————————
def load_base_data(filepath='model_input.json'):
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            return json.load(f)
    except Exception as e:
        logging.error(f"Ошибка при загрузке файла: {e}")
        sys.exit(1)


# ——————————————————————————————
# 3. Проверка и анализ внешних ссылок
# ——————————————————————————————
def enrich_external_links(links: list):
    enriched_info = []

    for url in links:
        logging.info(f"🌐 Обработка внешней ссылки: {url}")
        try:
            response = requests.get(url, timeout=10)
            soup = BeautifulSoup(response.text, 'html.parser')

            title = soup.title.string.strip() if soup.title else 'Без названия'
            desc_tag = soup.find("meta", attrs={"name": "description"}) or \
                       soup.find("meta", attrs={"property": "og:description"})
            description = desc_tag["content"].strip() if desc_tag and "content" in desc_tag.attrs else ""

            enriched_info.append({
                "url": url,
                "title": title,
                "description": description
            })

        except Exception as e:
            logging.warning(f"⚠ Не удалось обработать {url}: {e}")
            continue

    return enriched_info


# ——————————————————————————————
# 4. Главная функция
# ——————————————————————————————
def enrich_model(base_data):
    external_links = base_data.get("external_links", [])
    enriched_links_data = enrich_external_links(external_links)

    result = {
        "model_id": base_data.get("modelId", "unknown"),
        "tags": base_data.get("tags", []),
        "description": base_data.get("description", ""),
        "external_enriched": enriched_links_data,
        "source_url": base_data.get("source_url", "")
    }

    return result


# ——————————————————————————————
# 5. Сохранение результата
# ——————————————————————————————
def save_result(data, filepath='model_enriched.json'):
    try:
        with open(filepath, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        logging.info(f"✅ Обогащённые данные сохранены в {filepath}")
    except Exception as e:
        logging.error(f"Ошибка при сохранении: {e}")


# ——————————————————————————————
# 6. Точка входа
# ——————————————————————————————
if __name__ == "__main__":
    base = load_base_data()
    enriched = enrich_model(base)
    save_result(enriched)
```

## 🧩 Что можно добавить в следующих версиях

|Возможность|Краткое описание|
|---|---|
|`asyncio` + `aiohttp`|Для одновременного парсинга нескольких ссылок|
|Мультиязычный анализ|Если нужно извлекать тексты на разных языках|
|Выделение изображений|Парсинг `<img>` и `og:image` для превью|
|Markdown-вывод|Готовый текст для публикации в Telegram|
|Отправка в API|Автоматическая отправка enriched-данных в бота или базу|

---

## 🚀 Что нужно на вход:

- JSON-файл `model_input.json` вида:

```
{
  "modelId": "hunyuan3d/Hunyuan3D",
  "tags": ["video", "3d"],
  "description": "Demo for 3D generation",
  "external_links": ["https://hunyuan3d.github.io/"],
  "source_url": "https://huggingface.co/spaces/hunyuan3d/Hunyuan3D"
}
```

### ❓ **Можно ли сделать скриншот всей страницы, даже если она прокручивается?**

✅ **Да**, но с оговорками:

- Это требует **настоящего браузера**, не `requests` или `bs4`.
    
- Используется **headless-браузер**, например:
    
    - **Selenium + Chrome/Chromium**
        
    - **Playwright (гораздо быстрее и стабильнее)**
        
- В обоих случаях можно:
    
    - Открыть страницу.
        
    - Дождаться её полной загрузки.
        
    - Прокрутить (или захватить всю высоту).
        
    - Сделать **full-page screenshot**.

### ❓ **Можно ли вытащить видео и вьюверы с сайта (не обязательно YouTube)?**

🟡 **Можно, частично. Зависит от типа встраивания:**

|Тип видео / вьювера|Шанс извлечения|Как вытаскивать|
|---|---|---|
|`<video src="...">` или `<source>`|✅ Высокий|`bs4` или `Playwright`|
|`<iframe src="YouTube">`|✅ Высокий|распарсить `iframe`, достать ID|
|WebGL/3D viewers (Three.js, Sketchfab)|⚠ Частично|можно достать `iframe`, URL, но не содержимое|
|Приватные видео с blob-URL (`blob:...`)|❌ Низкий|нельзя получить напрямую — это потоки в браузере|
|Видео, грузящиеся через JS (XHR/Fetch)|⚠ Частично|нужен Playwright + сетевой мониторинг|

🧠 _Технически:_

- `Playwright` позволяет **перехватывать запросы к видеофайлам** (`.mp4`, `.webm`, `.m3u8` и т.д.).
    
- Это даёт возможность вытащить **реальный видео-URL**, если он передаётся явно.

## 🧠 План по продвинутой структуре `hugo_enricher_v0_4.py`

### 🧩 Основные модули:

|Модуль|Что делает|
|---|---|
|`load_base_data()`|Чтение входного JSON|
|`enrich_links_async()`|Асинхронно обходит все внешние ссылки|
|`extract_metadata()`|Заголовок, описание, изображения, видео|
|`screenshot_fullpage()`|Делает скрин всей страницы (через Playwright)|
|`build_markdown()`|Подготавливает пост в Telegram-стиле|
|`save_output()`|Сохраняет JSON и/или Markdown|
|`send_to_api()`|(опционально) шлёт результат в Telegram API|

## ⚙️ Требуемые библиотеки:

```
pip install playwright beautifulsoup4 aiohttp aiofiles
playwright install
```

## 🔧 Примеры работы:

- Ссылка: `https://hunyuan3d.github.io/`
    
- Снимок экрана: `screenshots/hunyuan3d.png`
    
- Видео найдено: `https://.../demo_video.mp4`
    
- Генерируется Telegram Markdown:

```
🧠 **Hunyuan3D**
Описание: Модель для 3D генерации из текста.
🌐 [Перейти на сайт](https://hunyuan3d.github.io/)
🎞 Видео найдено
🖼 Скриншот во вложении
```

## 👉 Что дальше?

Я могу:

1. Собрать весь `hugo_enricher_v0_4.py` **с Playwright + Markdown + видео**.
    
2. Разбить его на **модули** (если хочешь запускать по частям).
    
3. Протестировать на одном примере (`hunyuan3d`) и выдать готовую структуру под сервер.

## 🧱 **Цель `hugo_enricher_v0_4.py`:**

- На вход: JSON по одной модели из Hugging Face.
    
- На выход: расширенный JSON **с полным набором структурированной информации**:
    
    - Название и описание с сайта.
        
    - Обнаруженные **внешние ссылки** (сайт, YouTube, GitHub).
        
    - Извлечённые **изображения и видео**.
        
    - Снимок всей страницы (screenshot).
        
    - Markdown-превью (как черновик, не для отправки).
        
    - Все данные складываются в единую JSON-запись.

## 🚫 Что пока **не делаем:**

- ❌ Не отправляем в Telegram.
    
- ❌ Не вызываем LLM.
    
- ❌ Не обрабатываем тысячи моделей.

## 📦 Промежуточная цель:

> 📂 Собрать **20–30 файлов JSON**, каждый — это "обогащённая" модель.  
> 🔎 После этого — анализ полей: что извлекается стабильно, что редко, как нормализовать.


## 🧠 Будущая логика (чуть позже):

1. По заполненным ячейкам модели отправляются в ролевую **LLM-комнату**.
    
2. Она пишет текст от лица Хьюго.
    
3. Далее текст уходит в Telegram через HuggingFace Agent или сценарий n8n.
    
4. (При желании — Хьюго комментирует публикацию спустя 1–2 часа, имитируя свою активность.)
 
## 🛠 Что предлагаю сейчас:

Следующий шаг — я сгенерирую:

- `hugo_enricher_v0_4.py`  
    с **всеми функциями**:
    
    - Асинхронный сбор по ссылкам.
        
    - Снимок всей страницы.
        
    - Парсинг описания, заголовка, изображений, видео.
        
    - Markdown-превью.
        
    - Сохранение результата как `model_enriched.json`.