## 📦 На вход:

```
{
  "title": "Hunyuan3D-2",
  "url": "https://huggingface.co/spaces/tencent/Hunyuan3D-2",
  "readme": "...",
  "description": null,
  "tags": ["gradio", "region:us"]
}
```

Будет работать **на сервере**, **по очереди**, **по JSON из n8n**.  
Пишу на Python (можно и на Node, но Py проще для парсинга).

### 🤖 Что делает агент:

#### 🟢 1. Парсит HuggingFace страницу:

- Если `description` пустой — вытаскивает текст из `README`
    
- Ищет **все внешние ссылки** в `README`:
    
    - 🏠 Website
        
    - 🧪 GitHub
        
    - 🎞️ YouTube
        
    - 📄 PDF / Tech Report

#### 🟢 2. Переходит по внешним ссылкам (по очереди):

- **Парсит HTML** (через BeautifulSoup):
    
    - `title`, `h1`, `meta[name="description"]`, `og:title`, `og:desc`
        
    - картинки `img`, `og:image`, иконки
        
- ⚠️ Не делает scroll → скриншот будет "верхним", но его можно сохранить
    
- 💡 Если найдена YouTube ссылка → сохранить в поле `videos`
    
- 💡 Если найдены ключевые слова `3D`, `reconstruction`, `mesh`, `glb`, `upload image`, → добавить в `tags`

#### 🟢 3. Обогащает карточку:

```
{
  "external_site": "https://hunyuan3d.github.io/",
  "detected_tags": ["3D", "image-to-3D", "textured", "Tencent", "reconstruction"],
  "video_preview": "https://www.youtube.com/embed/xyz...",
  "screenshots": ["/assets/hunyuan3d_thumb.png"],
  "meta_description": "High-res 3D reconstruction from image using Tencent’s model",
  "markdown_block": "..." // готовый md-файл
}
```

### ⚙️ Библиотеки Python:

- `requests` + `bs4` (BeautifulSoup) → вытягиваем HTML
    
- `playwright` → если захотим рендерить JS и делать скрин
    
- `re` → ищем ключевые слова
    
- `markdownify` → если хочется конвертировать HTML в текст

### 🎁 Выход:

Сохраняем результат как:

- `.json` (полная обогащённая карточка)
    
- `.md` (человеческое описание)
    
- `.png` (скрин превью сайта, если надо)
    
- `assets/hunyuan3d/` — папка для картинок
 
### 🔮 А вот и волшебство:

> Ты можешь дать агенту сотню моделей, а он сам пойдёт, соберёт всё, что видит: **описание**, **превью**, **видео**, **осмысленные теги**, и сложит это красиво на полки твоей базы.

### 📌 Следующий шаг:

Хочешь — я подготовлю **каркас Python-скрипта Hugo-Enricher**, который можно будет запускать на сервере вручную или по cron'у.  
Потом можно превратить его в `n8n-exec`-модуль или webhook.

## Скрипт hugo_enricher.py

### 📦 Что делает этот скрипт:

- Загружает страницу модели HuggingFace
    
- Вытаскивает:
    
    - 🔗 внешние ссылки
        
    - 📝 заголовки, описания, Open Graph
        
    - 🏷️ ключевые смысловые теги (`3D`, `reconstruction`, `render`, и т.д.)

- Сохраняет результат в output/enriched_hunyuan3d.json

▶ Как запустить:

```
pip install requests beautifulsoup4
python hugo_enricher.py
```

### 📌 Что дальше:

- можно расширить скрипт: скриншоты, markdown, видео
    
- подключить к n8n через Webhook или cron
    
- сделать Хьюго-контрольный интерфейс
