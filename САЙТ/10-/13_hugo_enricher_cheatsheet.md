# 🧠 Hugo-Enricher: Установка и запуск (v0.1)

## 📁 Структура проекта

Рекомендуемая папка на сервере:
```
/home/Hugo/
├── hugo_enricher.py         # основной скрипт
├── output/                  # результаты
└── venv/                    # виртуальное окружение Python
```

## ⚙️ Установка Python-окружения

### 1. Установить venv:

```bash
apt update
apt install python3.12-venv -y
```

### 2. Создать виртуальное окружение:

```bash
cd /home/Hugo
python3 -m venv venv
```

### 3. Активировать venv:

```bash
source venv/bin/activate
```

### 4. Установить зависимости:

```bash
pip install beautifulsoup4 requests
```

## 🚀 Запуск скрипта:

```bash
cd /home/Hugo
source venv/bin/activate
python hugo_enricher.py
```

После запуска появится:

```
/home/Hugo/output/enriched_hunyuan3d.json
```

## 📌 Что делает v0.1:

- Загружает HTML HuggingFace модели
- Извлекает title, meta, OpenGraph описание
- Находит внешние ссылки
- Определяет ключевые слова (3d, reconstruction и т.п.)
- Сохраняет `.json` в `/output/`

## ⏭ Следующий шаг (v0.2):

- Генерация `.md` рядом с `.json`
- Скачивание `og:image` или `<img>` с внешнего сайта
- Без headless-браузера (пока!)
