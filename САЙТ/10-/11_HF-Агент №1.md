
## Cron - готовая нода синхронизации агента со временем

🔹 Как работает:

- Нода `Cron` или `Interval` запускает агента:
    
    - каждый день
        
    - раз в час
        
    - каждую пятницу в 18:00 и т.п.

 🟢 Плюсы:

- Ничего не нужно подключать
    
- Работает прямо в n8n
    
- Можно запускать любую цепочку по расписанию
    

🔴 Минусы:

- Нет визуального календаря
    
- Нельзя вручную "перенести" задачу на другой день
    
- Не виден список будущих событий

## 👇 Агент: HF-Agent-1

Он делает:

1. Запрос на Hugging Face
    
2. Отбирает модели по тегам
    
3. Фильтрует по дате (например, последние 24 часа)
    
4. Формирует JSON / пост / лог
    

### 🔄 Запуск:

- вручную (тест)
    
- через Cron
    
- по Webhook (другой агент)
    
- по кнопке (UI)
    
- по событию из календаря

### 💡 Совет

С точки зрения архитектуры лучше:

- **разделять агента и триггеры**
    
- тогда ты можешь подключить к агенту:
    
    - `Cron`
        
    - `Webhook`
        
    - `Telegram Command`
        
    - `Google Calendar`

## 🔧 Настройка `HTTP Request` ноды

1. Кликни по ноде `HTTP Request` — откроется её панель настроек справа
    
2. Введи следующие параметры:

| Поле                | Значение                                      |
| ------------------- | --------------------------------------------- |
| **HTTP Method**     | `GET`                                         |
| **URL**             | `https://huggingface.co/api/spaces?limit=100` |
| **Response Format** | `JSON` (по умолчанию должно быть)             |

📌 **Authentication:** оставь `None` — пока мы используем открытый API без токена.

### ✅ Что делает эта нода?

Она обращается к Hugging Face и запрашивает список Spaces (модельных приложений) — максимум 100 штук.

### ▶️ Проверка

1. Нажми **"Execute Node"** (вверху справа)
    
2. Внизу появится результат — **список объектов**, каждый со следующими полями:
    
    - `id` — имя space
        
    - `likes` — количество лайков
        
    - `lastModified` — дата обновления
        
    - `tags` — массив тегов (например: `video`, `image-to-video`, `diffusers`, и т.п.)

### 💡 Варианты для расширения позже:

- Добавить `authorization` токен Hugging Face, если понадобится больше данных
    
- Использовать пагинацию (`offset` и `limit`)
    
- Запрашивать `models`, а не `spaces` — если будешь работать с нейросетями напрямую

### Authentication: None

🔹 **Hugging Face API для `spaces` публичен** — тебе не нужен токен или авторизация, чтобы получить список Spaces с помощью запроса.

 ✅ Подтверждение:

Оставь так:

- `Authentication` = `None`
    
- Нажми **"Execute Node"**

🔍 Что ты должен увидеть:

- в `OUTPUT` справа появится **массив из объектов**
    
- каждый объект имеет поля: `id`, `tags`, `lastModified`, `likes` и т.п.

|Поле|Нужно сейчас?|Комментарий|
|---|---|---|
|**Send Query Parameters**|❌ Нет|Мы уже передаём `?limit=100` прямо в URL|
|**Send Headers**|❌ Нет|Авторизация не требуется|
|**Send Body**|❌ Нет|Метод `GET`, тело не используется|
|**Options**|❌ Не нужно|Пока не задаём таймауты и прочее|


 🔚 Вывод:

Ты всё сделал верно. Теперь:

1. Убедись, что метод `GET`
    
2. URL: `https://huggingface.co/api/spaces?limit=100`
    
3. Authentication: `None`
    
4. Остальное — выключено
    
5. Нажми **`Execute Node

## 🔹 Шаг 2: Добавляем ноду `Function` (Фильтрация по тегам)

### 🎯 Цель:

Оставить только те Spaces, которые содержат нужные теги, например:  
`video`, `image-to-video`, `diffusion`, `animation`, `music`, `voice`, `multimodal`

Ты сможешь менять список в любой момент.

## 🛠 Что делать:

1. Нажми `+` → выбери `Function`
    
2. Назови ноду: Filter by Tags

```
const tagsToInclude = ['video', 'image-to-video'];

return items.filter(item => {
  const tags = (item.json.tags || []).map(t => t.toLowerCase());
  return tags.some(tag => tagsToInclude.includes(tag));
});
```

Эта нода пропустит только те модели, которые помечены одним из нужных тегов. Дальше мы можем сужать фильтр, уточнять список тегов или добавлять исключения.

в n8n **нода `Function`** называется просто:

> 🟠 `Code` – Run custom JavaScript code

### ✅ 1. Что такое `Code`-нода в n8n?

Это JavaScript-скрипт, который обрабатывает данные **прямо внутри n8n**, в формате JSON.

#### Принцип работы:

- Получает **массив объектов** (`items`) от предыдущей ноды.
    
- Возвращает **новый массив объектов**, отфильтрованный или модифицированный.
    
- Пишется на **JavaScript**, но только для обработки данных внутри n8n.

📌 Это не серверный скрипт, а просто логика потока.

### 🐍 2. Чем отличается от Python-скриптов?

|JavaScript (n8n `Code`)|Python (на сервере)|
|---|---|
|Выполняется внутри ноды n8n|Запускается на уровне всей системы|
|Доступ к данным только из потока n8n (JSON)|Можно работать с файлами, внешними модулями|
|Используется для фильтрации, трансформации|Используется для сложных вычислений, парсинга и ИИ|

🔁 Принцип обработки **очень похожий** — и там и там: берёшь [[ТЕРМИНЫ, ТЕГИ/JSON]], что-то с ним делаешь, возвращаешь результат.

## 🟣 Шаг 3: Set — формируем красивые данные

Теперь добавим ноду `Set`, чтобы привести данные к удобному виду.

📦 Добавь новую ноду типа **`Set`** после Function.

### Пример настройки полей:

|Поле|Значение|
|---|---|
|`title`|`{{$json.id}}`|
|`url`|`https://huggingface.co/spaces/{{$json.id}}`|
|`likes`|`{{$json.likes}}`|
|`tags`|`{{ $json.tags.join(', ') }}`|
|`created_at`|`{{$json.createdAt}}`|
|`sdk`|`{{$json.sdk}}` (если хочешь, можно убрать)|

📍 Так мы сформируем стандартный набор, пригодный для публикации, базы данных или импорта в сайт/Telegram.

### Как это сделать в интерфейсе:

1. В ноде `Set`, включи `Keep Only Set` — ✅
    
2. Добавь поля:

| Name       | Value                                      | Тип    |
| ---------- | ------------------------------------------ | ------ |
| title      | {{$json.id}}                               | String |
| url        | https://huggingface.co/spaces/{{$json.id}} | String |
| likes      | {{$json.likes}}                            | Number |
| tags       | {{$json.tags.join(', ')}}                  | String |
| created_at | {{ $json.createdAt.split('T')[0] }}        | String |
Выражения {{$json.likes}} вносятся во вкладке Expression. 
n8n по умолчанию думает: раз это число, ты сам его напишешь. А `{{$json.likes}}` — это **выражение**, его надо интерпретировать, поэтому нужна ручная активация.

### 🧩 В `Set`-ноде:

Когда ты нажимаешь `Add Value`, тебе предлагают выбрать тип данных:

- **String** → текст (названия, ссылки, даты, теги и т.д.)
    
- **Number** → числовое значение (например, лайки)
    
- **Boolean** → true/false
    
- **Other** → JSON-объекты, массивы и т.п.

### ✅ Поэтому:

|Поле|Тип|Почему|
|---|---|---|
|`title`|String|Это строка, вроде `user/tool`|
|`url`|String|Это ссылка|
|`likes`|**Number**|Это число (например, `1093`)|
|`tags`|String|Мы делаем `join(', ')` — строка|
|`created_at`|String|Это дата в текстовом формате|

### 🎯 Вывод:

- Да, **всё, кроме `likes` — это `String`**.
    
- Только `likes` — это `Number`, потому что мы можем потом его сортировать, фильтровать и т.д.


> ⚠️ Но даже если ты по ошибке добавишь всё как `String` — ничего страшного не произойдёт, просто `likes` будет текстом вроде `"1093"`, а не числом `1093`. Это критично только если хочешь сортировать или вычислять.

