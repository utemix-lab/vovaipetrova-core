
# 🔄 04_N8N_AUTOMATION — Установка n8n и первые шаги автоматизации

## 🚀 Цель
Развернуть платформу автоматизации n8n, которая станет «нервной системой» проекта:
- соединит Obsidian, WordPress, Telegram, скрипты
- выполнит автоматическую обработку и публикацию
- со временем будет управлять ИИ-агентами

---

## 🐙 Почему n8n
- Open Source альтернатива Zapier/Make, но на сервере под твоим полным контролем.
- Легко расширяется через кастомные HTTP-запросы и скрипты.
- Гибко работает с API других сервисов.
- Поддерживает визуальный интерфейс построения сценариев (flow).

---

## 🛠 Установка через Docker Compose
Мы разместили docker-compose.yml в `/opt/n8n` со следующим содержанием:

```yaml
version: "3.1"

services:
  n8n:
    image: n8nio/n8n:0.230.0
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=superpass123
      - N8N_HOST=0.0.0.0
    volumes:
      - ./n8n_data:/home/node/.n8n
```

Где:
- `N8N_BASIC_AUTH_*` — сразу настроили базовую авторизацию для защиты от посторонних.
- `volumes` — сохраняют настройки и флоу, чтобы не терялись при перезапуске.

---

## 🔥 Запуск и проверка
```bash
cd /opt/n8n
docker compose up -d
docker ps
docker logs n8n-n8n-1
ss -tulpn | grep 5678
```

После этого открыли интерфейс в браузере:
```
http://87.228.88.243:5678
```
(логин: `admin`, пароль: `superpass123`)

---

## ✍️ Первые задачи для n8n
- Подключение Obsidian: сканировать папку с `.md` и преобразовывать в JSON/YAML для дальнейших публикаций.
- Автоматические POST-запросы в WordPress через REST API для создания статей.
- Публикация в Telegram: подготовка сообщений и отправка через бота.

---

## 💡 Возможные сценарии
- Автоматическое присвоение тегов, публикация и рассылка уведомлений.
- Генерация мини-анонсов, глитчевых изображений, коротких видео для Telegram.
- Загрузка файлов на CDN и обновление ссылок в WordPress.
- Планирование задач на базе cron внутри n8n.

---

## 🎭 Балаганный стиль
Мы рассматриваем автоматизацию не просто как «механизм», а как часть балаганного спектакля:
- у каждого процесса может быть «роль»,
- сценарии (flow) становятся актами,
- публикация в Telegram и на сайте — представление перед зрителем.

---

## 📜 Резюме
- n8n установлен в Docker, защищён логином и паролем.
- Порты и UFW настроены для доступа снаружи.
- Заложена основа для автоматизации: можно строить цепочки, подключать API и управлять контентом из Obsidian в WordPress и дальше в Telegram.

---
✅ Глава 04 завершена. Впереди — философия «руки и мозг» (скрипты и LLM), эвристики и первый театр ботов!
