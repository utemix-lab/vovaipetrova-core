
# 📦 01_SERVER_SETUP — Архитектура сервиса и выбор инфраструктуры

## 🚀 Цель
Построить гибкую инфраструктуру для личного проекта, включающего:
- Базу знаний (Obsidian)
- Визуальный сайт (WordPress)
- Систему автоматизации (N8N)
- В дальнейшем — ИИ-агентов и ролевой театр ботов

Для этого нужен VPS (виртуальный сервер), который позволит:
- Управлять системой на уровне командной строки
- Самостоятельно устанавливать Docker и Docker Compose
- Открывать необходимые порты для сервисов
- Легко масштабировать ресурсы

## 🐧 Почему Ubuntu
- Широко используется для серверов
- Огромное количество документации
- Простая установка Docker
- Минимальная нагрузка (нет лишнего GUI)

## 🔍 Почему Selectel
- Локальная регистрация и удобство с точки зрения законодательства и геополитики
- Возможность быстрой смены конфигурации
- Понятные тарифы и управление


## 💻 Локальная подготовка на Windows
### 🛠 Проблема локали Windows
Чтобы SSH, Git и Docker работали без багов с путями, ключами SSH и GitHub:
- Поставили **Windows с англоязычной локалью и настройками по умолчанию** (Avoid кириллица в путях и именах пользователей).

### ⚙ Зачем Git Bash
- **Git** — это только система контроля версий (для управления репозиториями).
- **Git Bash** — это терминал для Windows, который имитирует Linux Shell (bash).
  Позволяет удобно работать с командами `ssh`, `ls`, `cd`, `nano`, которые в стандартном CMD не поддерживаются.

### ⚙ Зачем WinSCP
- Для визуального копирования файлов между Windows и сервером через SFTP.
- Чтобы быстро закинуть/скачать конфиги, образы, дампы.

### ⚙ Зачем Visual Studio Code
- Для редактирования скриптов и файлов конфигурации локально, с подсветкой YAML, Bash, Dockerfile.
- Возможность подключаться по SSH к серверу прямо из VS Code (через Remote SSH).

## 🔐 Настройка SSH
- Сгенерировали ключи SSH, чтобы входить без пароля.
- Настроили SSH-клиент в Git Bash для соединения с сервером Ubuntu.

## 📜 Резюме
- Имеем сервер Ubuntu на Selectel с полным root-доступом через SSH.
- Локально подготовлена Windows-среда (английская), с Git Bash, WinSCP и VS Code для управления и разработки.
- Закладывается структура, где:
  - Сервер — это базовый каркас (backend),
  - Obsidian на локальной машине — палитра идей,
  - WordPress — визуальный холст (frontend),
  - N8N и Docker — связующее звено и автоматизатор всего балагана.

---

