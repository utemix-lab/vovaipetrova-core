
# 🐧 02_UBUNTU_DOCKER — Подготовка Ubuntu, установка Docker и настройка фаервола

## 🚀 Цель
Настроить на VPS инфраструктуру для запуска всех наших будущих сервисов:
- Docker для изолированного развёртывания приложений
- Docker Compose для управления множеством сервисов через один файл
- UFW для управления доступом к портам

Это даёт:
- Полную управляемость зависимостями
- Лёгкость переноса между серверами
- Безопасность через контейнеризацию

---

## 🔧 Подготовка сервера Ubuntu
Ubuntu — минималистичный дистрибутив, отлично подходящий для запуска Docker.  
На сервере установлен только базовый CLI (терминал), никаких окон.

Мы вошли через SSH с помощью ключей:
```bash
ssh root@ip_address
```

---

## 🐳 Установка Docker
### 📦 Сначала обновили пакеты
```bash
apt update
```

### 📦 Установили Docker (версия из репозитория Ubuntu)
```bash
apt install -y docker.io
systemctl start docker
systemctl enable docker
```

### 📌 Проверили
```bash
docker -v
```
Ожидали увидеть что-то вроде `Docker version 27.x`

---

## 🐙 Установка Docker Compose (v2 plugin)
Для удобства запустили так:
```bash
mkdir -p ~/.docker/cli-plugins/
curl -SL https://github.com/docker/compose/releases/download/v2.24.5/docker-compose-linux-x86_64 -o ~/.docker/cli-plugins/docker-compose
chmod +x ~/.docker/cli-plugins/docker-compose
```

Проверили:
```bash
docker compose version
```
Чтобы убедиться, что доступна команда `docker compose` (без дефиса).

---

## 🔥 Настройка фаервола UFW
По умолчанию UFW блокирует входящие соединения. Мы открыли нужные порты:

```bash
ufw allow OpenSSH
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 5678/tcp  # для n8n
ufw reload
ufw status verbose
```

---

## 🚀 Первый тест Docker
Для проверки запустили:
```bash
docker run hello-world
```
Чтобы убедиться, что Docker может загружать образы и запускать контейнеры.

---

## 🛠 Полезные команды для управления
- Список запущенных контейнеров:
```bash
docker ps
```
- Логи контейнера:
```bash
docker logs <container_id>
```
- Список открытых портов:
```bash
ss -tulpn
```

---

## 🎭 Почему Docker и Compose
- Docker позволяет запускать приложения изолированно от основной системы. Никаких конфликтов библиотек.
- Compose даёт возможность описать весь стэк в одном файле (`docker-compose.yml`), автоматически поднять сеть, тома и зависимости.
- После перезагрузки сервер автоматически перезапускает контейнеры (через `restart: always`).

---

## 📜 Резюме
- Имеем сервер Ubuntu, готовый к работе с Docker.
- Настроен UFW для доступа к SSH, HTTP/HTTPS и будущему порту n8n.
- Проверен Docker и Compose.

Всё готово для следующего шага — установки и настройки n8n.

---
✅ Глава 02 завершена. Впереди — автоматизация через n8n и конфигурация рабочих процессов!
