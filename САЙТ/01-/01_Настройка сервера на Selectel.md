
## 🔧 Что тебе нужно выбрать:

### 👉 **Облачные серверы** (в разделе «Вычисления»)

Это и есть нужный нам тип ресурса: **виртуальные машины (VM)**, которые можно гибко масштабировать, снабдить внешним IP, подключить Docker, WordPress, ботов и т.п.

> ✅ Жми **"Облачные серверы"**, затем **создать инстанс / виртуальную машину.**

### Другие пункты, которые **может понадобятся позже**:

- **Объектное хранилище** — для хранения файлов, резервных копий, медиа от WordPress, экспорта из Obsidian.
    
- **Container Registry** — если будешь публиковать свои Docker-контейнеры.
    
- **CDN / DNS-хостинг** — ускорение доступа к сайту, настройка поддоменов.
    
- **Мониторинг** — метрики по серверу (CPU, трафик, диски).
    
- **Менеджер секретов** — хранение токенов для ботов, API-ключей.

### Не нужно для твоего проекта сейчас:

- **VDS Selectel** — старый тип изоляции, не нужен.
    
- **Managed Kubernetes** — избыточно для одного проекта.
    
- **Готовое облако 1С, Облако на базе VMware** — не по теме.
    
- **AI-маркетплейс, ML-платформа, Inference-платформа** — если не запускаешь свои ML‑модели на GPU.

Если ты нажмешь «Облачные серверы» → тебе предложат выбрать:

- Регион (Санкт-Петербург)
    
- Образ (Ubuntu 22.04)
    
- Размер сервера (CPU, RAM, диск)
    
- IP
    
- Пароль или SSH-ключ

## 🧩 Что такое **пул (зона доступности)**?

**Пул** (или **availability zone**) — это физически изолированный сегмент дата-центра в выбранном регионе. У региона (например, _Санкт-Петербург_) может быть несколько пулов: `ru-3a`, `ru-3b`, `ru-1a` и т.д.

Это нужно, чтобы:

- **распределять нагрузку** между пользователями;
    
- **обеспечивать отказоустойчивость** (можно разместить резервный сервер в другом пуле);
    
- **увеличивать доступность ресурсов** — если в одном пуле закончились нужные конфигурации, можно взять в другом.

## 🔍 Какой пул выбрать?

Если ты **создаёшь первый сервер и не строишь сложную архитектуру**, то **выбирай любой доступный пул**, например:

- `ru-3a` или `ru-3b` — часто рекомендуемые по умолчанию;
    
- или смотри, в каком пуле доступны нужные конфигурации CPU/памяти.

💡 Иногда бывает так: в одном пуле нет нужной конфигурации (например, 4 vCPU + 16 ГБ RAM), а в другом — есть. Тогда просто меняй пул до тех пор, пока не станет доступен нужный размер.

## 🔐 Резюме

|Вопрос|Ответ|
|---|---|
|**Что такое пул?**|Зона внутри региона (дата-центра)|
|**Нужно ли заморачиваться?**|Нет, выбирай любой доступный|
|**Что делать, если не хватает ресурсов?**|Попробуй другой пул (например, `ru-1b` вместо `ru-3b`)|
Выбрал Data Science VM (Ubuntu 22.04 LTS 64-bit) и 2 vCPU / 8 ГБ.
Номер аккаунта 449591
Пароль 1P!
ssh/id_ed25519
Новый SSH-ключ
Имя - Traci
Публичный ключ:
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEOd19jezG6sVoVOT/8FL3oWVoxTWDU2sDWhxNUBuVWQ vova@DESKTOP-B61J4UO

Когда сервер создастся, для подключения из Windows пиши:
ssh -i C:\Users\Администратор\sshkeys\id_ed25519 root@<IP>
(замени `<IP>` на публичный адрес сервера, который даст Selectel).

Публичный ключ 87.228.88.243
