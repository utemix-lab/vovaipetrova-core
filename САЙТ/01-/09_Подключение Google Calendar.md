
## ✅ Шаг 1. Создание проекта в Google Cloud Console

1. Перейди на: https://console.cloud.google.com/
    
2. Нажми **"Создать проект"** → дай имя (например, `n8n-calendar`).
    
3. После создания — выбери проект (в шапке справа).
    
4. Перейди в меню **API и сервисы → Библиотека**.
    
5. Найди и включи:
    
    - **Google Calendar API**
        
    - (опционально: Google Drive API, если хочешь работать с вложениями)

## ✅ Шаг 2. Настройка OAuth2-клиента

1. Меню **"Учетные данные" (Credentials)** → нажми **"Создать учетные данные" → OAuth 2.0 Client ID**
    
2. Тип приложения: **Web Application**
    
3. Имя: `n8n OAuth`
    
4. В разделе **Authorized redirect URIs** добавь:

```
http://localhost:5678/rest/oauth2-credential/callback
```

(если ты используешь n8n локально, порт может отличаться)

5. Сохрани → появятся **Client ID** и **Client Secret**

## ✅ Шаг 3. Добавление в n8n (Credentials)

1. Открой n8n → перейди в **Credentials → Google Calendar OAuth2 API**
    
2. Назови, например: `Google Calendar Main`
    
3. Заполни поля:
    
    - **Client ID** — скопируй из Google Cloud
        
    - **Client Secret** — тоже
        
    - **Scopes** — вставь:

```
https://www.googleapis.com/auth/calendar
```

4. Нажми **"Connect account"**
    
5. Авторизуй, выбрав свою Google-учетку
    
6. Готово! Теперь это соединение можно использовать в нодах.

## ✅ Шаг 4. Пример использования в n8n

### 🔹 Получить события:

1. Создай новый workflow
    
2. Добавь `Google Calendar → Get All Events`
    
3. Выбери `Credential` (которую только что создал)
    
4. Укажи:
    
    - Календарь (можно выбрать по имени)
        
    - Диапазон дат (например, `Next 7 days`)

### 🔹 Реагировать на событие:

1. Добавь `Google Calendar Trigger`
    
2. Выбери `Credential`
    
3. Укажи ID календаря
    
4. Укажи тип события: `Event Created`, `Event Updated`, `Event Deleted`
 

---

## 💡 Полезные советы

- 📆 ID календаря — это обычно твой email (`you@gmail.com`) или UUID, если календарь создан вручную.
    
- 🧠 Один агент может создавать события в календаре другого, если календари открыты для доступа.
    
- 📥 Можешь парсить описание события и вшивать туда инструкции для агента (`tag:HF-Scan`, `deadline:2025-07-20`).
    

---

Хочешь — сделаю шаблон базового агента, который:

1. По событию из календаря запускается,
    
2. Читает описание события,
    
3. Действует по инструкции (например, мониторит HF, постит в Telegram и т.п.).

## **Google Calendar** — мощный, но иногда избыточный для простого планирования.

Не сработало из-за отсутствия домена
