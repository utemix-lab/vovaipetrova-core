
# 🛡️ Шпаргалка: Настройка SSH-доступа через ключ с нуля (WinSCP + сервер)

## 🧾 Сценарий
Ты пересоздал сервер, хочешь подключаться через WinSCP **по SSH-ключу без пароля**. Вот полный путь.

---

## 🪜 Этап 1: Генерация ключей на ПК (в PuTTYgen)

1. Открой `PuTTYgen` (ставится вместе с WinSCP)
2. Отметь:  
   **`Type of key to generate:` → `EdDSA` → `Ed25519`**
3. Нажми **Generate** — и крути мышкой в белом поле, пока не сгенерируется ключ
4. Не вводи passphrase (оставь пустым)
5. Сохрани:
   - **Private key**: `Save private key` → `id_ed25519.ppk`
   - **Public key**: Скопируй строку вида:  
     `ssh-ed25519 AAAAC3... комментарий`

💡 *WinSCP использует только `.ppk` (приватный ключ). Публичный нужен только на сервер.*

---

## 🪜 Этап 2: Вставка публичного ключа на сервер

В браузерной консоли сервера (вошёл как `root`):

```bash
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
```

📋 Вставь туда скопированный публичный ключ (ПКМ или Shift+Insert)

### Важно про `nano`:
- Это редактор, работающий в текстовом режиме
- **Ctrl+O** — сохранить (может не сработать, если Windows перехватывает)
- **Enter** — подтвердить имя файла
- **Ctrl+X** — выйти
- Если всё глючит — можно заменить на `echo 'ключ' >> ~/.ssh/authorized_keys`

### Права:
```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

---

## 🪜 Этап 3: Настройка WinSCP

1. Открыть WinSCP
2. Ввести:
   - `Protocol`: SFTP
   - `Host`: IP сервера
   - `Port`: 22
   - `User`: root
   - `Password`: оставить пустым!
3. Нажать **Advanced**
   - Перейти в `SSH → Authentication`
   - В поле **Private key file** указать путь к `id_ed25519.ppk`
   - Убедиться, что **Pageant** и **agent forwarding** — отключены

Нажать OK → Login.

---

## ⚠ Особенности WinSCP

- **Поле "Password" всегда показывает одинаковое число точек** — это фейк, не ориентируйся по ним.
- Чтобы очистить пароль — просто много раз нажми `Backspace` или `Delete`.
- При подключении по `.ppk` пароль не нужен вообще.

---

## ✅ Проверка входа

Если всё сделано правильно — WinSCP подключится к серверу **без пароля** и покажет файлы (`/root/`).

---

## 💡 Дополнительно (по желанию)

Если хочешь — можешь потом:
- Отключить вход по паролю:
  В `/etc/ssh/sshd_config` установить:

  ```
  PasswordAuthentication no
  ```
  и перезапустить:
  ```bash
  systemctl restart ssh
  ```

- Хранить ключи в `C:\Users\...\.ssh\` и делать бэкап `id_ed25519.ppk`

---

👍 Всё, теперь у тебя стабильный, безопасный и автономный доступ к серверу!
