Новый пароль: DuXASb5TCtRF
Публичный ключ:
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEJsd2BwXP9O8yvtNRVYrSfbZyrv/h2qA5VKVQDskqqp eddsa-key-20250725

Вход на сервер через [[Git Bash]]:

```
ssh root@87.228.88.243
```

```
ssh vovaipetrova
```

Активируй виртуальное окружение
```
cd /hdd/llama/text-generation-webui
source venv/bin/activate
```


```
cd /hdd/llama/text-generation-webui/models
mkdir LLaMA-2-7B-GGUF
cd LLaMA-2-7B-GGUF

wget https://huggingface.co/TheBloke/Llama-2-7B-Chat-GGUF?show_file_info=llama-2-7b-chat.Q5_K_M.gguf
```

http://87.228.88.243:7860
http://87.228.88.243:5678
http://87.228.88.243:39263

HF токен:
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

Сейчас ты общаешься с **LLaMA 3 8B**, запущенной **локально на твоем сервере**, через **Text-generation WebUI**
http://87.228.88.243:5678:7860

Лицензионный ключ n8n:
1c7d25e7-f62a-4f73-9373-67f01c79fccc

N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=1234


Путь к .ssh:
C:\Users\utemix\.ssh

## ✅ Как войти в `venv`

Сначала перейди в папку с проектом (если ещё не там):

```
cd /home/Hugo
```

Затем активируй среду:

```
source venv/bin/activate
```

После этого ты увидишь в начале строки:

```
(venv) root@vovaipetrova:/home/Hugo#
```

### 🔁 Напомню:

Если ты выходишь из venv или перезагружаешь сервер — активируй его снова перед запуском:

```
cd /home/Hugo
source venv/bin/activate
```

## Управление Docker
### docker-compose.yml
### Найти `docker-compose.yml`

Выполни команду из домашней директории:

```
find / -name docker-compose.yml 2>/dev/null

```

Она покажет путь, где уже существует файл. Например, результат может быть:
/root/n8n/docker-compose.yml

### 📄 Что такое `docker-compose.yml`?

Это **конфигурационный файл**, в котором описано, **как запускать контейнеры Docker**. В твоём случае — как запускать `n8n`.

Он позволяет:

- Задать **образ** (`n8nio/n8n`),
    
- Назначить **переменные окружения** (например, логин/пароль),
    
- Пробросить **порты** (5678 наружу),
    
- Указать **объём данных** (где хранится база workflow),
    
- Настроить **автозапуск**, сети, зависимости и прочее.
    

---

### 🧠 Почему мы его создавали?

Чтобы ты мог:

- Автоматически запускать n8n при перезагрузке сервера;
    
- Удобно управлять сервисом: `docker compose up`, `down`, `logs`, `restart`;
    
- Задать переменные, такие как:
    
    - `N8N_BASIC_AUTH_USER` — логин
        
    - `WEBHOOK_TUNNEL_URL` — внешний адрес
        
    - `DB_TYPE`, `TZ`, `EXECUTIONS_DATA_SAVE_ON_SUCCESS` — опции работы n8n
        

---

### 🛠️ Пример: без `docker-compose.yml` всё пришлось бы писать вручную

Пример ручного запуска:

bash

КопироватьРедактировать

`docker run -it --rm \   -p 5678:5678 \   -e N8N_BASIC_AUTH_USER=admin \   -e N8N_BASIC_AUTH_PASSWORD=pass \   n8nio/n8n`

А с `docker-compose.yml` — всё просто:

bash

КопироватьРедактировать

`docker compose up -d`

---

### 📦 Итого:

| Без `docker-compose`    | С `docker-compose.yml`                   |
| ----------------------- | ---------------------------------------- |
| Много ручных параметров | Всё описано в одном YAML-файле           |
| Трудно редактировать    | Легко поменять порты, пароли, переменные |
| Неудобно обновлять      | Удобно перезапускать и масштабировать    |

### 🧾 Что значит расширение `.yml`?

`.yml` — это просто **расширение файла YAML**  
**YAML (Yet Another Markup Language)** — читаемый человеку формат, похожий на отступы в Python:

```
ключ: значение
настройка:
  вложенная: да
```

> В Docker используется YAML-файл `docker-compose.yml`, чтобы описывать конфигурацию контейнеров.

`.yml` и `.yaml` — **одно и то же**, можно использовать любое расширение.

### 🛠️ Для чего этот файл?

Файл `docker-compose.yml`:

- описывает **один или несколько контейнеров** (сервисов),
    
- позволяет запускать их **вместе, как единую систему**.

То есть в **одном файле** ты можешь описать:

```
services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: secret
```

В этом примере ты запускаешь сразу два контейнера: `n8n` и `postgres`.

### 🤖 Запускаю я **все контейнеры сразу** или можно по отдельности?

Когда ты пишешь:

```
docker compose up -d
```

— запускаются **все контейнеры**, описанные в этом `docker-compose.yml`.

Но можно запустить **только один**:

```
docker compose up -d n8n
```

Или только `postgres`:

```
docker compose up -d postgres
```

Также можно **остановить только один**:

```
docker compose stop n8n
```

### 📦 Нужны ли разные файлы для разных контейнеров?

**Зависит от архитектуры** проекта:

|Ситуация|Один файл|Разные файлы|
|---|---|---|
|Все контейнеры связаны (например, n8n и база)|✅ Лучше один файл|❌ Неудобно|
|Разные, независимые проекты|❌ Лучше разделить|✅ Да|

Примеры:

- Если у тебя есть n8n и PostgreSQL, которые работают **вместе** — можно держать в одном `docker-compose.yml`.
    
- Если на сервере ещё, например, WordPress или HuggingFace API — лучше сделать **отдельные папки с отдельными compose-файлами**.

### 📁 Как это может выглядеть на практике:

```
/opt/
  n8n/
    docker-compose.yml
  wordpress/
    docker-compose.yml
  openrouter/
    docker-compose.yml
```

Ты заходишь в нужную папку и запускаешь:

```
cd /opt/n8n
docker compose up -d

cd /opt/wordpress
docker compose up -d
```

### 🔧 Бывают ли ситуации, когда нужно запустить не все?

Да. Например:

- Тебе нужно только `n8n`, но не `postgres` — запускаешь `n8n`.
    
- Временно отключить Telegram-бота — останавливаешь только его.
    
- Запускаешь `postgres` позже (задержка по сети или настройкам).

### 🧠 Вывод:

- `.yml` — читаемый конфигурационный файл.
    
- Один файл может описывать **один или много контейнеров**.
    
- Лучше использовать **отдельные файлы** для **независимых проектов**.
    
- Можно запускать **все или отдельные** сервисы из файла.

## Надо ли вручную запускать Docker каждый раз при входе на сервер?

**Нет, не нужно.** Когда ты запускаешь команду:

```
docker compose up -d
```

– контейнер запускается в **фоновом режиме** (`-d = detached`) и **продолжает работать даже после выхода из терминала или перезагрузки сессии**.

### ✅ А чтобы запускался при перезагрузке сервера:

Добавь в `docker-compose.yml` строчку:

```
restart: unless-stopped
```

Пример:

```
services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    restart: unless-stopped
```

### 🛠️ Вопрос 2: Как **отключать бота** на время отладки?

Идеально ты подметил — когда автоматизация уже работает, удобно **отключать конкретные части**, например Telegram-бота, чтобы не шёл в мир:

#### 🔹 Способ 1: Остановить контейнер

Если бот живёт в отдельном контейнере (например, `telegram-bot`), отключаешь так:

```
docker compose stop telegram-bot
```

А потом включаешь обратно:

```
docker compose start telegram-bot
```

#### 🔹 Способ 2: Комментировать в `docker-compose.yml`

Можно просто временно закомментировать блок:

```
# telegram-bot:
#   image: ...
#   ...
```

И перезапустить:

```
docker compose up -d
```

#### 🔹 Способ 3: В самом n8n — поставить условие

Например, если переменная `DEBUG=true`, то Telegram-узел не срабатывает. Можно использовать **условную логику** или **временное отключение ноды** в Workflow.

## В чём разница между директорией и **контейнером**?

|Директория|Контейнер|
|---|---|
|Это **папка** на диске сервера|Это **виртуальная система** (микро-Linux со своим окружением)|
|Содержит **файлы проекта** (например, `docker-compose.yml`)|Содержит **запущенный процесс**, изолированный от остальных|
|Ты сам её создаёшь и редактируешь|Контейнер создаётся по инструкции (`Dockerfile`, `image`)|
|Постоянна|Временный, но может сохранять состояние через volume|
|Пример: `/opt/n8n/`|Пример: `n8n-n8n-1` (в `docker ps`)|

### 📌 Пример:

- У тебя есть **директория** `/opt/n8n/`, в ней лежит `docker-compose.yml`.
    
- Когда ты запускаешь:

```
cd /opt/n8n/
docker compose up -d
```

— запускается **контейнер**, например `n8n-n8n-1`.

Он живёт независимо, как будто это мини-программа в своём окружении.

### 🧠 Выводы:

- 🔁 Docker можно настроить на **автозапуск**, ты не обязан запускать его вручную.
    
- 🧩 Ты можешь **временно отключать** любой сервис (например, Telegram-бота) без остановки всей системы.
    
- 📁 **Директория** — место на диске, где хранятся настройки. **Контейнер** — запущенный экземпляр программы.


## Мой docker-compose.yml

✅ Всё, что у тебя есть — правильно:

```
version: "3.1"

services:
  n8n:
    image: n8nio/n8n:0.230.0
    restart: always                          # 🔁 Автозапуск при перезагрузке — ✅
    ports:
      - "5678:5678"                           # 🌐 Порт проброшен наружу — ✅
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true            # 🔒 Авторизация включена — ✅
      - N8N_BASIC_AUTH_USER=admin             # 👤 Логин — ✅
      - N8N_BASIC_AUTH_PASSWORD=superpass123  # 🔐 Пароль — ✅
      - N8N_HOST=0.0.0.0                      # 📡 Разрешён внешний доступ — ✅
    volumes:
      - ./n8n_data:/home/node/.n8n           # 💾 Данные сохраняются — ✅
```

## /opt/n8n

Это **директория на диске**, не сам Docker. Это просто **папка на твоем сервере**, в которой лежит файл `docker-compose.yml`.

Эта папка **нужна не Docker-движку, а тебе**, чтобы:

- хранить `docker-compose.yml`,
    
- подключать постоянные тома (`volumes`) для хранения данных n8n,
    
- удобно управлять контейнером.

### 🧱 А где сам **Docker**?

Docker — это не файл или папка, это **демон (служба)**, установленная на сервере.  
Он управляет:

- контейнерами (изолированные среды),
    
- образами (templates),
    
- томами (volume — хранилища данных),
    
- сетями.


Ты можешь управлять им через команды, независимо от места хранения `docker-compose.yml`.

### 🔁 Как всё работает вместе:

Представь:

```
/opt/n8n/
├── docker-compose.yml     ← инструкция для запуска контейнера
├── n8n_data/              ← папка с сохранёнными данными n8n (volume)
```

Когда ты пишешь:

```
cd /opt/n8n
docker compose up -d
```

Docker:

- читает файл `docker-compose.yml`,
    
- скачивает образ `n8nio/n8n`,
    
- запускает контейнер с нужными переменными,
    
- монтирует `./n8n_data` внутрь контейнера как хранилище (`/home/node/.n8n`).

### 🔧 Где живут данные и образы Docker?

Docker по умолчанию хранит свои данные тут:

|Что|Где по умолчанию|
|---|---|
|Контейнеры|`/var/lib/docker/containers/`|
|Образы|`/var/lib/docker/overlay2/`|
|Томы (volumes)|`/var/lib/docker/volumes/`|

Ты их обычно не трогаешь руками — Docker сам управляет этим.

### 🧠 Вывод:

- 📁 `/opt/n8n` — **твоя рабочая папка с настройками**, не сам Docker.
    
- 🐳 Docker — это **служба**, которая управляет контейнерами по файлу `docker-compose.yml`.
    
- 🔌 Связь происходит, когда ты выполняешь `docker compose` из этой папки.

## 🐳 Что такое Docker?

**Docker — это программа (демон)**, установленная на твоём сервере (например, Ubuntu), которая:

- управляет контейнерами,
    
- запускает их из образов,
    
- отслеживает их состояние,
    
- управляет сетями, томами, образами и пр.

👉 **Docker сам не живёт в контейнере**. Он как **менеджер или операционная система для контейнеров**, установленный напрямую в Linux.

## 📦 Что такое контейнер?

**Контейнер** — это **изолированная мини-система**, основанная на образе.  
Каждый контейнер работает **как отдельное приложение**, но с меньшими затратами, чем виртуальная машина.

Например, контейнер `n8n-n8n-1`:

- основан на образе `n8nio/n8n:0.230.0`
    
- запускает внутри себя `n8n`
    
- использует том `./n8n_data` как диск

## 🔁 Так что значит перезапустить контейнер?

Это не перезапуск Docker в целом.  
Ты перезапускаешь **один конкретный контейнер**, а Docker-движок продолжает работать в фоне.

## ✅ Как перезапустить контейнер n8n

Предположим, ты находишься в папке `/opt/n8n`, где лежит `docker-compose.yml`.

Вот **самый правильный способ**:

```
cd /opt/n8n
docker compose down
docker compose up -d
```

### Что происходит:

- `docker compose down` — останавливает и удаляет контейнер (не данные).
    
- `docker compose up -d` — запускает **новый контейнер** на основе того же `docker-compose.yml`.

## 🛠 А если хочешь жёсткий перезапуск с обновлением переменных

```
docker compose down --volumes
docker compose up -d --force-recreate
```

- `--volumes` — удалит старые тома (если они не сохранены как внешняя папка, как у тебя).
    
- `--force-recreate` — точно создаст **новый** контейнер, а не повторно запустит старый.

> Но у тебя используется `./n8n_data` как том, так что данные сохранятся.


### 🧠 Итого:

|Что|Описание|
|---|---|
|Docker|Программа, управляющая контейнерами (работает в фоне как демон)|
|Контейнер|Изолированное приложение, запущенное внутри Docker|
|Перезапустить контейнер|Значит **перезапустить одно конкретное приложение**, не весь Docker|
|Перезапустить Docker|Это как перезагрузка всего менеджера, редко нужно: `systemctl restart docker`|


Схема уровней:  
🖥️ Сервер → 🐳 Docker → 📦 Контейнер → ⚙️ Приложение (n8n)

## 🧱 Что такое «выполнить команду внутри контейнера»?

Контейнер — это как **мини-компьютер внутри твоего сервера**. Он работает отдельно, у него свой внутренний мир (переменные, окружение, путь, файлы и т.д.).

Когда ты вводишь команду:

```
docker exec -it n8n-n8n-1 printenv | grep N8N_AUTHENTICATION
```

Ты **не заходишь внутрь**, а просто **даёшь команду контейнеру снаружи**:

> «Эй, контейнер `n8n-n8n-1`, покажи мне свои переменные окружения, содержащие слово `N8N_AUTHENTICATION`».

### 🔧 Что именно делает эта команда:

- `docker exec` — говорит Docker: «Выполни команду внутри контейнера».
    
- `-it` — позволяет выполнять команды в интерактивном режиме (будто ты туда "зашёл").
    
- `n8n-n8n-1` — имя контейнера, в котором работает n8n.
    
- `printenv` — команда Linux, которая выводит переменные окружения.
    
- `| grep N8N_AUTHENTICATION` — фильтр: покажи только те, где есть `N8N_AUTHENTICATION`.

### 💡 Альтернатива: "зайти внутрь" контейнера

Если хочешь реально «провалиться» внутрь, как в подлодку:

```
docker exec -it n8n-n8n-1 bash
```

После этого ты окажешься внутри:

```
root@container-id:/home/node#
```

И уже внутри можно выполнять обычные команды Linux:

```
printenv | grep N8N
```

Чтобы выйти из контейнера — просто набери:

```
exit
```

### 📌 Так что «выполнить внутри контейнера» — это:

✅ или дать разовую команду (`docker exec`)  
✅ или зайти внутрь (`docker exec ... bash`) и поработать напрямую
