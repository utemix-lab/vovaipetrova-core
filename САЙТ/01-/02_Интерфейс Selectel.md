
## 🖥 Левое меню (основная навигация)

- **Серверы**  
    👉 Здесь ты управляешь своими виртуальными машинами (создаёшь новые, останавливаешь, удаляешь, видишь их состояние).
    
- **Диски**  
    👉 Управление отдельными дисками (можешь добавлять новые, подключать их к серверу, удалять).
    
- **Сеть**  
    👉 Управление IP-адресами, приватными сетями, подсетями. Здесь же можно подключать плавающие IP.
    
- **Файрволы**  
    👉 Создание и настройка правил доступа (какие порты открыты, для каких IP).
    
- **Группы безопасности (NEW)**  
    👉 Более высокая абстракция файрволов — можешь создать группу правил и применять её к разным серверам сразу.
    
- **Образы**  
    👉 Хранятся твои шаблоны ОС. Можно создать образ существующего сервера и потом из него быстро разворачивать другие.
    
- **Бэкапы**  
    👉 Управление резервными копиями серверов.
    
- **Файловое хранилище**  
    👉 Объектное хранилище для файлов (по типу Amazon S3). Используется для хранения бэкапов, медиа и др.
    
- **Балансировщики**  
    👉 Если нужно распределять нагрузку между несколькими серверами.
    
- **Kubernetes / Container Registry / Базы данных**  
    👉 Более продвинутые услуги для контейнеров и управляемых баз.
    
- **Менеджер секретов**  
    👉 Для хранения паролей, токенов, SSH-ключей.
    
- **API**  
    👉 Для автоматизации через HTTP-запросы (скрипты, CI/CD).

## 🗂 Основная область (твоя страница «Серверы»)

- **Фильтр по городу и зоне (Санкт-Петербург / ru-3)**  
    👉 Можно выбирать датацентр и регион, если у тебя их несколько.
    
- **Поиск по имени / UUID / IP**  
    👉 Для быстрого поиска серверов.
    
- **Карточка сервера `vovaipetrova`**  
    Здесь видишь:
    
    - **Имя** сервера
        
    - **UUID** (идентификатор)
        
    - **Ресурсы** (CPU, RAM, диск)
        
    - **ОС** (Ubuntu 24.04)
        
    - **Публичный IP** (87.228.88.243)
        
    - Статус **ACTIVE** — сервер работает
        
    - Переключатель для включения/выключения сервера (зелёная кнопка)

## 🔧 Что дальше и куда нажимать

- ✅ **Три точки справа на карточке сервера (`⋮`)**  
    👉 Тут меню управления: перезагрузка, выключение, удаление, управление настройками.
    
- ✅ **«Подключить публичный IP»**  
    👉 Если нужен ещё один IP для сервера (у тебя уже один есть).
    
- ✅ **«Создать сервер»** (кнопка справа вверху)  
    👉 Для создания нового.
    
- ✅ **«Создать» под бэкапами, файрволами и хранилищем**  
    👉 Быстрые кнопки для создания этих ресурсов.

## 📝 Что советую сделать сначала

1. Перейди в **«Файрволы»** слева — создай правила, чтобы были открыты только нужные порты (например 22 для SSH и 80/443 для веба).
    
2. Затем в **«Серверы»** открой **`⋮`** рядом с твоим сервером → выбери **«Подключение»**, там будет SSH-команда для входа (типа `ssh root@87.228.88.243`).
    
3. После этого можно переходить к настройке внутри Ubuntu через SSH (обновления, fail2ban и т.д.).

## 🚀 Твои первые действия в Selectel после создания сервера

### 1️⃣ Создай **Файрволы**

Чтобы открыть только нужные порты и заблокировать всё лишнее.

🔹 **Где?**

- В меню слева выбери **«Файрволы»**.
    

🔹 **Что делать?**

1. Нажми **«Создать файрвол»** (или «Создать группу правил» — смотря какой интерфейс покажет).
    
2. Задай имя, например: `my-server-rules`.
    
3. Добавь правила:
    
    - **Allow SSH (порт 22, TCP)**
        
        - направление: Входящий
            
        - источник: `0.0.0.0/0` (разрешит вход с любых IP, потом можно ограничить своим IP)
            
    - **Allow HTTP (порт 80, TCP)**
        
        - для сайтов без HTTPS
            
    - **Allow HTTPS (порт 443, TCP)**
        
        - для защищённых сайтов
            
4. По умолчанию остальные порты будут закрыты.
    
5. Сохрани.

🔹 **Примени правила**

- Перейди в «Серверы» → нажми ⋮ → «Изменить файрвол» → выбери свой `my-server-rules`.

### 2️⃣ Подключись по **SSH**

🔹 **Где найти команду?**

- Перейди в «Серверы».
    
- Нажми **⋮ справа от твоего сервера → Подключение**.
    
- Там будет пример вроде:

```
ssh root@87.228.88.243
```

(или может сразу кнопка скопировать).

### 3️⃣ Подключись через терминал

На своей машине открой терминал и вставь:

```
ssh root@87.228.88.243
```

Если первый раз подключаешься — спросят **Yes/No**, напиши `yes`.  
Затем введи **пароль**, который прислал Selectel (или был показан после создания сервера).

### 4️⃣ Настрой сервер внутри Ubuntu

Сразу после входа можно:

✅ **Обновить пакеты**

```
apt update && apt upgrade -y
```

✅ **Поставить fail2ban для защиты SSH**

```
apt install fail2ban -y
```

(по умолчанию уже неплохо работает, можно потом докрутить конфиг).

## 🎯 РЕЗЮМЕ (шпаргалка)
1. Selectel → Файрволы → Создать:
   - Allow TCP 22 (SSH)
   - Allow TCP 80 (HTTP)
   - Allow TCP 443 (HTTPS)

2. Применить файрвол к серверу.

3. Перейти в Серверы → ⋮ → Подключение → скопировать ssh root@IP.

4. На своей машине:
   ssh root@87.228.88.243
   (ввести пароль)

5. Внутри сервера:
   apt update && apt upgrade -y
   apt install fail2ban -y
💪 Всё, после этого сервер уже защищён базово и готов для следующего:

- установки nginx, docker, wordpress или чего захочешь.

Если готов — скажи, и пойдём пошагово ставить **Nginx** или что у тебя в планах. 🚀

### 🧐 Почему тут **«Приватная подсеть»**, если ты создавал публичный сервер?

Потому что в Selectel **файрволы настраиваются именно на уровне приватных подсетей** (VPC / Private Subnet).  
То есть:

- твой сервер физически находится в приватной сети (внутренней),
    
- а публичный IP (87.228.88.243) — это просто плавающий IP (floating IP), «приклеенный» к твоему серверу для доступа из Интернета.

Файрволы в Selectel (в их интерфейсе) настроены так, что:

- ты фильтруешь **трафик в приватную подсеть**, которая заодно включает трафик с твоим публичным IP (через NAT-правила облака).

### 🚩 Почему пишет: «У вас пока нет подсетей с портами роутера»?

Это значит, что у тебя сейчас **сервер создан без явно выделенной приватной сети**, которую можно было бы выбрать здесь для применения «группы правил файрвола».

Это **особенность их GUI**:

- ты всё равно защищён базовым Security Group, который создаётся автоматически,
    
- но для продвинутых файлов (тех, что на вкладке «Файрволы») нужен явно настроенный «облачный роутер + подсеть».

### 💡 Что это значит для тебя сейчас?

👉 **Не переживай.**  
Для твоего текущего сервера правила безопасности задаются не через этот «Файрвол» на уровне VPC, а через:

- **Security Groups**, которые прикреплены к самому серверу при создании (их можно посмотреть и настроить на карточке сервера).

У Selectel это чуть запутано:

- «Группы безопасности» (Security Groups) управляют доступом на уровне интерфейсов сервера (по портам и IP).
    
- «Файрволы» в этом разделе интерфейса — это отдельный сервис, который больше похож на edge firewall (для сценариев с виртуальными роутерами и приватными сетями).

### 🚀 Что делать тебе?

✅ Перейди в интерфейсе Selectel в **«Группы безопасности»** (на твоей панели слева чуть ниже «Файрволов» — у тебя там даже написано `New`),  
там:

- найди группу безопасности, которая применена к твоему серверу (по умолчанию часто `default`),
    
- отредактируй правила (разреши входящие подключения на порты 22, 80, 443).

🎯 **Итог:**

- Этот «Файрвол» с приватной подсетью — для продвинутой инфраструктуры (когда у тебя сетка VPC, VPN, несколько подсетей).
    
- А тебе сейчас достаточно настроить **Security Group**, прикреплённую непосредственно к твоему серверу.

## 🚀 Как отредактировать правила в этой «default» группе безопасности

1️⃣ **Найди свою группу:**

- На твоем скриншоте видна группа **`default`** (Санкт-Петербург / ru-3).
    
- В ней сейчас **6 правил**, и она уже применена к твоему серверу (1 сервер).

2️⃣ **Открой её для редактирования:**

- Кликни **по названию группы `default`**.  
    (Не на три точки — там другое. Нужно именно кликнуть по строке или имени.)

3️⃣ **Попадёшь внутрь группы**, где будет список существующих правил (например `allow-icmp`, `deny-any` и т.д.).

## ➕ Добавляем правила для портов 22, 80 и 443

4️⃣ Найди кнопку типа **«Добавить правило»** или **«Add rule»** (интерфейс может чуть меняться).  
Чаще всего она находится где-то вверху или под таблицей правил.

Создай три правила (по одному на порт):

### 🔸 Правило для SSH

- **Протокол:** TCP
    
- **Port:** `22`
    
- **Source:** `0.0.0.0/0` (чтобы разрешить со всех IP)
    
- **Action:** Allow

### 🔸 Правило для HTTP

- **Протокол:** TCP
    
- **Port:** `80`
    
- **Source:** `0.0.0.0/0`
    
- **Action:** Allow

### 🔸 Правило для HTTPS

- **Протокол:** TCP
    
- **Port:** `443`
    
- **Source:** `0.0.0.0/0`
    
- **Action:** Allow

Сохрани изменения.

## ✅ После этого

- Все входящие подключения по SSH (22), HTTP (80) и HTTPS (443) будут разрешены.
    
- Сервер уже готов к подключению по SSH и для работы сайтов.

## 🚀 Подключаемся по SSH

Теперь тебе нужно зайти на свой сервер из терминала. Вот пошагово:

### 1️⃣ Открыть терминал

На Windows можно использовать:

- **PowerShell**
    
- **cmd**
    
- или лучше всего — **Windows Terminal** (если установлен).

Если у тебя Linux или Mac — открывай обычный терминал.

### 2️⃣ Подключиться по SSH

Введи команду (используй свой IP):

```
ssh root@87.228.88.243
```

(это IP, который у тебя был указан в интерфейсе Selectel).

### 3️⃣ Принять fingerprint

При первом подключении он спросит:

```
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

Напиши yes и нажми Enter.
### 4️⃣ Ввести пароль

- Введи пароль root, который тебе показал Selectel при создании сервера (или прислал на почту).
    
- При вводе пароля в терминале **ничего не будет отображаться** (ни звёздочек, ни точек) — это нормально. Просто печатай и нажми Enter.

## ✅ Если всё прошло успешно

Ты окажешься в консоли Ubuntu с приветствием вида:

```
Welcome to Ubuntu 24.04 LTS
...
root@vovaipetrova:~#
```

### 🥳 Дальше — первые команды

Готов?  
Если да, то запускаем сразу после входа:

```
apt update && apt upgrade -y
```

(обновит список пакетов и поставит последние версии).

Затем:

```
apt install fail2ban -y
```

(чтобы защитить SSH от перебора паролей).

C:\Users\Администратор>ssh root@87.228.88.243
root@87.228.88.243: Permission denied (publickey).
Отлично, давай спокойно разберёмся.  
Это **классическая ситуация**, у новичков она возникает почти всегда.

## 🚩 Почему так?

Ошибка Permission denied (publickey) говорит, что сервер **ожидает SSH-ключ**, а не пароль.  
По умолчанию Ubuntu 24.04 на Selectel часто развёртывается с настройкой **только по SSH-ключу**, без пароля root.
## 🔍 Что проверить в Selectel

Перейди в **Selectel → Серверы → vovaipetrova**, открой подробности и посмотри:

- **Есть ли там поле «Пароль root»**?
    
    - Если **нет** (или там написано «для подключения используйте SSH-ключ»), значит твой сервер создавался с ключом.
        
- **Есть ли там пункт «SSH-ключи»** в информации о сервере?
    
    - Если указан какой-то SSH-ключ, значит нужен приватный ключ на твоём компьютере.

## 🚀 Что можно сделать сейчас

### 📌 Вариант 1. Использовать SSH-ключ

Если ты **при создании сервера выбирал SSH-ключ**, тогда подключаться нужно так:

```
ssh -i /path/to/private_key root@87.228.88.243
```

Где `/path/to/private_key` — путь к твоему приватному ключу (`.pem` или `.id_rsa`).

Например на Windows:
ssh -i C:\Users\Администратор\.ssh\id_rsa root@87.228.88.243
(Файл приватного ключа ты должен был сохранить при его генерации.)