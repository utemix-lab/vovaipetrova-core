
# 🛠️ Шпаргалка: Переподключение VS Code по SSH с ключами

## 📁 1. Какие ключи бывают и где они хранятся

| Название файла       | Назначение                         | Где используется                |
|----------------------|------------------------------------|----------------------------------|
| `id_ed25519`         | Приватный ключ (OpenSSH формат)    | VS Code, Git Bash                |
| `id_ed25519.pub`     | Публичный ключ (OpenSSH формат)    | Добавляется на сервер (в `~/.ssh/authorized_keys`) |
| `id_ed25519.ppk`     | Приватный ключ для PuTTY/WinSCP    | Используется WinSCP              |
| `config`             | Конфиг файл для `ssh`              | Упрощает подключение (алиасы)    |
| `known_hosts`        | Хранит проверенные хосты           | Автообновляется при первом подключении |

Хранятся обычно в: `C:/Users/<имя_пользователя>/.ssh/`

---

## 🖥️ 2. Подключение по SSH через Git Bash

1. Открой Git Bash.
2. Выполни команду:

```bash
ssh -i ~/.ssh/id_ed25519 root@IP_АДРЕС_СЕРВЕРА
```

(если используешь `~/.ssh/config`, достаточно:)

```bash
ssh vovaipetrova
```

---

## 🧠 3. Если видишь ошибку "REMOTE HOST IDENTIFICATION HAS CHANGED!"

Удалить старую запись:

```bash
ssh-keygen -R <IP-адрес сервера>
```

или вручную стереть строку из файла `~/.ssh/known_hosts`.

---

## 🧩 4. VS Code (Remote - SSH)

1. Установи расширение **Remote - SSH**.
2. В файле `~/.ssh/config` добавь:

```
Host vovaipetrova
    HostName 87.228.88.243
    User root
    IdentityFile C:/Users/utemix/.ssh/id_ed25519
    IdentitiesOnly yes
    ServerAliveInterval 60
    ServerAliveCountMax 5
```

3. В VS Code нажми `F1` → `Remote-SSH: Connect to Host...` → выбери `vovaipetrova`.

---

## 🧷 5. WinSCP

1. Загрузи приватный ключ `.ppk` в поле "Private key file".
2. Проверяй: логин — `root`, порт — `22`.
3. Включи опцию **"keyboard-interactive"** (если используешь пароль при входе).

---

## 💡 Важно помнить

- **VS Code подключается долго** — это нормально при первом запуске, он копирует утилиты на сервер.
- **Публичный ключ (`.pub`) не нужен локально**, но **должен быть на сервере** в `~/.ssh/authorized_keys`.
- Если ключи были пересозданы — нужно **удалить старые строки** из `known_hosts`.

---

