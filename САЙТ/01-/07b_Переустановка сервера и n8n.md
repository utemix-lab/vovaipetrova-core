
### 🛠️ **Сделаем переустановку сервера и n8n с нуля**

**1. Отформатируем диск и подготовим `/hdd/`**

**2. Установим:**

- Node.js подходящей версии (лучше 20.x)
    
- npm
    
- n8n **локально** (в `/hdd/n8n`)
    
- PM2 **локально**, а не глобально (это важно)
    

**3. Настроим запуск:**

- через `./node_modules/.bin/pm2 start ./node_modules/n8n/bin/n8n`
    
- добавим `.env`
    
- убедимся, что `NODE_PATH` видит `./node_modules`
    

**4. Настроим автозапуск через pm2 save + startup**

**5. Обеспечим:**

- сохранность всех данных в `/hdd/`
    
- отсутствие глобальных конфликтов и мусора в `/`