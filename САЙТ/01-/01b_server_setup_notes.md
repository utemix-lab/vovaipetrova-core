
# Настройка сервера Selectel и SSH-доступ (13 июля 2025)

## 📌 Что было сделано

1. Установили **англоязычную Windows**, чтобы пути в Git, SSH и Docker отображались корректно.
2. Установили **Git for Windows** (с Git Bash), чтобы использовать ssh-keygen и ssh-copy-id.

---

## 🔑 Настройка SSH-ключей

### Генерация ключей на локальном компьютере
```bash
ssh-keygen -t ed25519 -C "me@mydomain.com"
```
- Сохранили ключ в `C:\Users\utemix\.ssh\id_ed25519` (по умолчанию).
- Приватный ключ: `id_ed25519` (никому не показывать).
- Публичный ключ: `id_ed25519.pub` (для сервера).

---

## 🚀 Настройка сервера (Selectel)

### Поиск и открытие консоли сервера
- В панели Selectel выбрали проект **My First Project**.
- Переключились на **Все регионы**, нашли сервер `vovaipetrova`.
- Открыли **веб-консоль (Console)** для входа напрямую на сервер.

### Логин на сервер
- Зашли под `root` и сгенерировали новый пароль через кнопку **[Сгенерировать новый]**.
- Вошли в систему через консоль (root + новый пароль).

---

## 🚀 Добавление SSH-ключа на сервер

### Создали папку и добавили ключ
```bash
mkdir -p ~/.ssh
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAGa04toy//2dfXexVoNk42GqyaEhELNVH1/n271o3EI me@mydomain.com" > ~/.ssh/authorized_keys
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

---

## ✅ Проверка подключения с локального компьютера

### Подключение по SSH без пароля
```bash
ssh root@87.228.88.243
```
- Подключение прошло успешно без ввода пароля.

---

## ⚡ Резюме
Теперь ты можешь всегда подключаться к серверу этой командой:
```bash
ssh root@87.228.88.243
```
Твои ключи уже настроены и будут работать, даже после перезагрузки компьютера или сервера.

---

📝 *Файл подготовлен автоматически 13 июля 2025.*
