### ✅ **Формат `united-states-flag.png` — это:**

- **Унифицированно**: всё в нижнем регистре — не нужно думать, как система обрабатывает `USA` vs `Usa` vs `usa`.
    
- **Безопасно для URL и файловых систем**: тире (`-`) — это стандартное разделение в URL, в файловых именах, в Slug'ах.
    
- **Читаемо человеком**: лучше, чем `united_states_flag` и, тем более, чем `UnitedStatesFlag`.
    
- **Совместимо с папками**: ты можешь иметь `flags/united-states-flag.png` и `countries/united-states.md`.
    

---

### 🧠 Стиль: где что использовать

|Тип|Формат|Пример|
|---|---|---|
|**Файл флага**|`lowercase-kebab`|`united-states-flag.png`|
|**Папка страны**|`lowercase-kebab`|`united-states/`|
|**Страница/slug**|`lowercase-kebab`|`/countries/united-states`|
|**Отображаемое имя**|`Title Case`|`United States`|
|**Русское имя**|`lowercase` или `Title Case`|`сша` / `США`|

---

### 🚀 Пример JSON-мэппинга:

```
{
  "сша": {
    "eng": "United States",
    "slug": "united-states",
    "flag": "united-states-flag.png"
  },
  "германия": {
    "eng": "Germany",
    "slug": "germany",
    "flag": "germany-flag.png"
  }
}
```

Такой формат идеален и для бэка, и для фронта, и для ботов.
