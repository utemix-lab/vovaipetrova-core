
# vovaipetrova-core

> Machine-readable description of the core project structure, logic, and automation components.

## [meta]
project_name: vovaipetrova-core  
language: ru / en  
status: active  
focus: automation, content orchestration, agent-based workflows  
integration: HuggingFace, OpenRouter, Telegram, Obsidian, WordPress  
llm_target: true

---

## [description]
Этот репозиторий содержит основной каркас проекта «Вова и Петрова».

Проект объединяет:
- AI-агентов с разными функциями (поиск, обогащение, публикация);
- структуру хранения данных, синхронизированную с базой знаний;
- автоматизацию с помощью `n8n` (или альтернативных workflow-движков);
- публикацию в Telegram и на сайт;
- взаимодействие реального пользователя с персонажами через игровые элементы.

## 
## [agents]
- `Hugo`: отслеживает модели HuggingFace, извлекает данные, обогащает описания
- `Vova`: собирает информацию о VST-инструментах по производителям
- (другие агенты в разработке)

## [technologies]
- OS: Ubuntu 24.04
- Server: Selectel VPS, mounted HDD at `/hdd`
- Code editors: VS Code + Remote SSH
- Automation: `n8n` (native setup, no Docker)
- AI APIs: OpenRouter, HuggingFace Spaces
- DB: SQLite (локальная), .md+frontmatter (база знаний)

## [automation]
- Рабочие цепочки на базе `n8n`
- Подключение агентов через JSON-конфигурации
- Обмен с Telegram через Telegram Bot API
- Экспорт `.md` в Obsidian + WordPress

## [llm_usage]
- README структурирован для разбора LLM
- Используются блоки `[...]`, чёткие списки, словари
- Frontmatter или YAML-подобная структура может быть добавлена в будущем

## [todo]
- [ ] Добавить README в папки агентов
- [ ] Обновить `config.json` и `manifest.json` для агентов
- [ ] Тестирование auto-sync с сервером
- [ ] Интеграция с GitHub Actions для логов

---

### ⚠️ **Что значит "This diff contains a change in line endings from 'LF' to 'CRLF'"?**

Это значит, что:

- В оригинальном `README.md` использовались **Unix-концовки строк** (`LF`, Line Feed).
    
- При сохранении в Windows (в твоём случае — через VS Code), строки были автоматически сохранены в формате **Windows (`CRLF`, Carriage Return + Line Feed)**.
    

---

### 💡 Это критично?

Нет, **не критично для Markdown или кода**, особенно если ты работаешь один.

Но:

- Если ты будешь работать в команде, особенно с людьми на Linux/macOS, такие различия могут "засорять" коммиты.
    
- Некоторые линтеры/CI могут придираться к неконсистентным окончаниям строк.
    

---

### ✅ Как исправить или избежать:

**В VS Code** ты можешь:

1. Открыть файл `README.md`.
    
2. В правом нижнем углу увидеть: `CRLF` — кликни на это.
    
3. Выбери `LF`.
    
4. Сохрани файл (`Ctrl+S`).
    

Теперь GitHub Desktop перестанет ругаться.

---

### 🛠 Альтернативно (глобально для проекта):

Можно создать файл `.gitattributes` в корне репозитория с таким содержанием:

```
* text=auto eol=lf
```

Это скажет Git автоматически конвертировать окончания строк в `LF` при коммитах, независимо от системы.