## 🧠 Что делает Tag-master

Когда база уже набрана (или хотя бы частично заполнена), Tag-master сможет:

### 🔎 1. **Проанализировать описание, категории, формат**

– Извлечёт потенциальные теги из текста:  
`steel string`, `nylon`, `loop-based`, `arpeggio`, `Kontakt` и т.д.

### 🧠 2. **Оценить повторяемость и релевантность**

– Например, если 20 инструментов уже используют `#Steel_String`, и новый инструмент соответствует — тег присваивается.

### 🧹 3. **Фильтрует и нормализует теги**

– Преобразует `steel string` → `#Steel_String`  
– Исключает повторяющиеся или дублирующие теги  
– Следит за стилем:

- Каждое слово с заглавной
    
- `Realistic` → `#Realistic_Guitar`, а не просто `#Realistic`
    

### 🔗 4. **Создаёт кросс-связи**

– Например:  
`#Guitar` + `#Steel_String` + `#Kontakt` → видно, что это реалистичный инструмент на движке Kontakt.

---

## 🤖 Почему **позже** — лучше:

На старте база ещё «не говорит сама за себя». Когда появится хотя бы:

- 50+ инструментов,
    
- несколько десятков тегов,
    
- перекрестные связи...
    

...Tag-master сможет **не только предлагать**, но и **контролировать семантическую чистоту** тегов — избегать дублей, ошибочных синонимов и мусора.

---

## 💡 А пока:

Пусть каждый агент (например, Вова) просто:

- **Генерирует логичные теги по описанию**
    
- **Форматирует их по правилам**
    
- **Не дублирует то, что явно не относится**
    

А Tag-master потом соберёт все теги, **сопоставит, объединит, нормализует** и выстроит крутую связанную сеть.
