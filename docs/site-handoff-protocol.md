---
title: Site Handoff Protocol — Think Tank ↔ Static Site
slug: site-handoff-protocol
summary: >-
  Протокол двустороннего порта для экспериментов визуала/структуры сайта
  отдельным IDE-агентом на основе прототипа (Think Tank). Без серверной логики,
  статический режим (Static First).
tags:
  - Автоматизация
  - Проектирование
machine_tags:
  - action/build
  - product/services
  - product/site
  - theme/automation
  - theme/graphics
status: ready
---

# Site Handoff Protocol — Think Tank ↔ Static Site

## TL;DR

Протокол двустороннего порта для экспериментов визуала/структуры сайта отдельным IDE-агентом на основе прототипа (Think Tank). Без серверной логики, статический режим (Static First).

---

## 1. Scope

**Статический сайт (Static First), без бэкенда.**

- Роль switch и артефакты: `localStorage` + hash-routes
- Без форм — только `mailto:` и Telegram deep links
- Все данные в JSON-файлах, рендеринг на клиенте

---

## 2. Формат обмена: IN/OUT

### IN (от прототипа к сайту)

Передача данных от Think Tank прототипа (`/prototype`) к статическому сайту:

- **Content slices:** KB JSONL, Stories digests JSONL
- **Routes:** маршруты с метаданными (title, OG, sitemap)
- **Sitemap:** XML для поисковиков
- **OG tags:** Open Graph метаданные для соцсетей
- **Styles tokens:** цвета, отступы, типографика

### OUT (от сайта к прототипу)

Обратная связь от статического сайта к прототипу:

- **UX-узкие места:** найденные проблемы взаимодействия
- **Решения по IA:** предложения по информационной архитектуре
- **Список адаптаций:** необходимые изменения в прототипе

---

## 3. Форматы данных

### Content

#### `kb_glossary_lite.jsonl`

Строки JSONL с терминами базы знаний:

```json
{"slug":"aliases","title":"Aliases","lite_summary":"Синонимы и альтернативные названия...","link":"kb/aliases.md"}
```

**Расположение:** корень репозитория (`kb_glossary_lite.jsonl`)

#### `stories_digests.jsonl` (опционально)

Строки JSONL с дайджестами Stories:

```json
{"slug":"digest-2026-01","title":"Stories Monthly Digest: Январь 2026","episodes":[...],"generated_at":"2026-01-11T12:00:00Z"}
```

**Расположение:** `prototype/data/stories_digests.jsonl` (если создан)

### Routes

#### `routes.json`

Маршруты сайта с метаданными:

```json
{
  "version": 1,
  "updated": "2025-11-18",
  "routes": [
    {
      "path": "/",
      "title": "Главная",
      "description": "индексы и быстрый ответ на вопрос «Что это?»",
      "og": {
        "title": "Vova & Petrova — Главная",
        "description": "...",
        "image": "/og/home.png"
      },
      "in_sitemap": true,
      "entries": [
        {
          "slug": "vova-i-petrova",
          "doc": "docs/vova-i-petrova.md",
          "status": "ok"
        }
      ]
    }
  ]
}
```

**Расположение:** `prototype/data/routes.json`

### UI Tokens

#### `tokens.json`

Дизайн-токены для стилей:

```json
{
  "colors": {
    "bg": "#f6f7fb",
    "card-bg": "#ffffff",
    "text": "#1f2933",
    "muted": "#64748b",
    "border": "#e3e8f4",
    "ready": "#0f9960",
    "review": "#d9822b",
    "draft": "#7b8794"
  },
  "spacing": {
    "xs": "4px",
    "sm": "8px",
    "md": "16px",
    "lg": "24px",
    "xl": "32px"
  },
  "typography": {
    "font-family": "Inter, Segoe UI, system-ui, sans-serif",
    "line-height": 1.5
  }
}
```

**Расположение:** `prototype/data/tokens.json` (создать при необходимости)

### Components

#### `components.md`

Описание UI-компонентов и их вариантов:

```markdown
# UI Components

## Header
- Props: logoSrc, menu, role, onRoleChange, artifactsCount
- Variants: default, compact

## RoleSwitch
- Props: value, options, onChange
- Options: [{id:"novice", label:"Новичок", icon:"👶"}, ...]

## NavBar
- Props: items, activeRoute, onNavigate
- Variants: horizontal, vertical, mobile
```

**Расположение:** `prototype/components.md` (создать при необходимости)

---

## 4. Процедуры

### Открытие Issue/PR на передачу решения

1. **Создать Issue** с меткой `ia:handoff`:
   - Заголовок: краткое описание изменения
   - Тело: описание проблемы/решения, ссылки на файлы
   - Labels: `ia:handoff`, при необходимости `ia:breaking`

2. **Создать PR** с изменениями:
   - Заголовок: `ia: [краткое описание]`
   - Тело: ссылка на Issue, список изменённых файлов
   - Labels: `ia:handoff`, при необходимости `ia:breaking`

### Breaking Changes

Изменения, которые требуют обновления прототипа:

- Изменение структуры `routes.json`
- Изменение формата `tokens.json`
- Удаление/переименование компонентов
- Изменение API `localStorage` для артефактов

**Метка:** `ia:breaking` — обязательна для таких изменений.

### SLA ответов

- **Время ответа:** 48 часов
- **Один PR за раз:** не открывать новый PR, пока предыдущий не закрыт/смёрджен

---

## 5. Статический режим

### Role Switch

Переключение ролей через `localStorage`:

```javascript
// Сохранение роли
localStorage.setItem('vp_role', 'client');

// Загрузка роли
const role = localStorage.getItem('vp_role') || 'novice';
```

### Артефакты

Корзина артефактов в `localStorage`:

```javascript
// Сохранение
localStorage.setItem('vp_artifacts', JSON.stringify({
  items: [
    {id: 'service-video', type: 'service', title: 'Видеопродакшн', ...}
  ]
}));

// Загрузка
const artifacts = JSON.parse(localStorage.getItem('vp_artifacts') || '{"items":[]}');
```

### Hash Routes

Навигация через hash:

- `/` — главная
- `/#services` — услуги
- `/#kb` — база знаний
- `/#think-tank` — Think Tank

### Контакты (без форм)

- **Email:** `mailto:contact@example.com?subject=Заявка`
- **Telegram:** `tg://resolve?domain=username` или `https://t.me/username`

---

## 6. Отчётность

### Мини-отчёт в Notion

После завершения передачи решения:

1. **Абзац:** краткое описание изменений и результата
2. **Список файлов/ссылок:**
   - Изменённые файлы
   - Ссылка на PR
   - Ссылка на Issue (если был)

**Пример:**

> Реализована передача маршрутов из прототипа в статический сайт через `routes.json`. Добавлены OG-метаданные для главной страницы и раздела услуг. Прототип обновлён с учётом обратной связи по UX навигации.
>
> Файлы:
> - `prototype/data/routes.json` (обновлён)
> - `prototype/data/tokens.json` (создан)
> - PR: #123
> - Issue: #456

---

## 7. Связанные документы

- [Think Tank — компактное ядро](think-tank-kompaktnoe-yadro.md)
- [UI макет — шапка и первый экран (Static First)](ui-maket-shapka-i-pervyj-ekran-static-first.md)
- [Процесс: Обсудили → Разложили → Связали](process-obsudili-razlozhili-svyazali.md)
- [Single Source Playbook](SINGLE-SOURCE-PLAYBOOK.md)

---

## 8. Чек-лист передачи

### От прототипа к сайту (IN)

- [ ] Обновлён `kb_glossary_lite.jsonl` (если изменились термины)
- [ ] Обновлён `routes.json` с актуальными маршрутами
- [ ] Обновлён `sitemap.xml` (через `npm run sitemap:generate`)
- [ ] Обновлены OG-метаданные в `routes.json`
- [ ] Обновлён `tokens.json` (если изменились стили)
- [ ] Обновлён `components.md` (если изменились компоненты)

### От сайта к прототипу (OUT)

- [ ] Создан Issue с меткой `ia:handoff`
- [ ] Описаны UX-узкие места
- [ ] Предложены решения по IA
- [ ] Составлен список необходимых адаптаций
- [ ] Создан PR с изменениями (если требуется)
- [ ] Добавлен мини-отчёт в Notion

---

**Версия:** 1.0
**Дата:** 2026-01-11
**Статус:** Активный протокол
