# Workspace экосистемы Vova & Petrova

## Описание

Workspace файл `vovaipetrova-ecosystem.code-workspace` объединяет все проекты экосистемы для удобной работы.

## Структура

### 📦 Core (Think Tank)
**Путь:** `vovaipetrova-core/`  
**Роль:** Ядро системы
- База знаний (Markdown файлы)
- Процессы и документация
- Экспорт данных для сайта
- Скрипты нормализации и линтинга

### 🌐 Site (Витрина)
**Путь:** `vovaipetrova-site/`  
**Роль:** Статический сайт
- Независимая структура сайта
- Интерфейс витрины проекта
- Импорт данных из core
- GitHub Pages деплой

## Связи и зависимости

### Направление данных: Core → Site

```
vovaipetrova-core/
├── kb_glossary_lite.jsonl          → vovaipetrova-site/data/
├── prototype/data/stories_digests.jsonl → vovaipetrova-site/data/
├── static/routes.json              → vovaipetrova-site/data/
├── static/tokens.json               → vovaipetrova-site/data/
└── static/components.md             → vovaipetrova-site/data/
```

### Процесс синхронизации

1. **В Core:** Генерация данных
   ```bash
   npm run content:slices          # Генерация content slices
   npm run static:routes:generate  # Генерация routes.json
   ```

2. **В Site:** Импорт данных
   ```bash
   npm run import  # Копирование данных из core
   ```

3. **В Site:** Сборка
   ```bash
   npm run build  # Сборка статического сайта
   ```

## Задачи (Tasks) в workspace

### 🚀 Полный цикл: Core → Site → Build
Запускает весь процесс синхронизации и сборки.

### Отдельные задачи:
- **📦 Core: Генерация content slices** — создаёт JSONL файлы
- **📦 Core: Генерация static routes** — создаёт routes.json
- **🔄 Синхронизация: Core → Site** — импортирует данные в site
- **🌐 Site: Сборка** — собирает статический сайт
- **✅ Core: Проверка качества** — линтинг документации

## Использование

1. Откройте workspace в Cursor/VS Code:
   ```
   File → Open Workspace from File → vovaipetrova-ecosystem.code-workspace
   ```

2. Используйте задачи для синхронизации:
   - `Ctrl+Shift+P` → `Tasks: Run Task` → выберите задачу

3. Работайте с обоими проектами одновременно:
   - Core — для работы с данными и процессами
   - Site — для работы с интерфейсом витрины

## Настройки workspace

- **Кодировка:** UTF-8 для всех файлов
- **Исключения:** node_modules, dist, tmp файлы
- **Markdown:** автоматический перенос строк
- **Форматирование:** отключено (используем скрипты)

## Рекомендации

1. **Перед работой с Site:** всегда запускайте синхронизацию из Core
2. **После изменений в Core:** обновите content slices перед импортом в Site
3. **Перед коммитом:** проверьте качество через линтинг
