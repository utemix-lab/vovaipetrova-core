### **Через git-bash добавим файлы:**

```
cd /o/DU

# Добавить ВСЕ файлы
git add .

# Commit
git commit -m "Обновление базы знаний с ПК"

# FORCE push (перезаписать GitHub твоей версией)
git push origin main --force
```
