**JSON (JavaScript Object Notation)** — это **текстовый формат обмена данными**, который используется для хранения и передачи структурированной информации между системами и приложениями.  Это не исполняемый код. То есть:

> ❌ JSON нельзя "запустить"  
> ✅ JSON можно **прочитать**, **изменить**, **анализировать** — в скриптах или визуально

 👨‍💻 Что можно делать с `.json` в VS Code:

|Что делать?|Как?|
|---|---|
|🔍 Удобно читать|VS Code форматирует JSON автоматически|
|🧠 Понимать структуру данных|Видишь ключи: `external_links`, `meta`, `tags`, `image_url` и т.д.|
|✍️ Редактировать вручную|Прямо в редакторе, как ты сейчас сделал|
|🔬 Проверить на ошибки|VS Code подскажет синтаксические ошибки|
|⚙️ Использовать в Python-скриптах|Например: `json.load()` читает JSON в виде словаря Python|

📌 Пример: как Python использует JSON

Ты можешь сделать мини-скрипт и запускать из VS Code:

```
import json

with open("output/enriched_hunyuan3d.json", "r", encoding="utf-8") as f:
    data = json.load(f)

print(data["external_links"])
```



### Структура и синтаксис

Данные в JSON представлены в виде **пар «ключ-значение»**. Ключи всегда записываются как строки в двойных кавычках, а значения могут быть разного типа: строки, числа, булевы значения, объекты, массивы или null. 

Основные структуры данных:

- **Объекты** — неупорядоченные коллекции пар «ключ-значение», заключённые в фигурные скобки {}. 
- **Массивы** — упорядоченные списки значений, заключённые в квадратные скобки .   

Объекты и массивы могут быть вложенными друг в друга, что позволяет создавать сложные структуры данных. 

### Преимущества и недостатки

**Преимущества JSON**:

- простота чтения и записи данных;
- компактность, что снижает объём передаваемых данных;
- поддержка многих языков программирования.

**Недостатки JSON**:

- отсутствие поддержки комментариев, что может затруднить документирование данных; 
- ограниченная поддержка бинарных данных; 
- отсутствие явной схемы данных, что может быть проблемой в случаях, когда требуется строгое определение структуры. 

### Области применения

JSON используется в различных сферах, например:

- **Обмен данными между сервером и клиентом** в веб-приложениях через API (например, RESTful API).
- **Хранение конфигураций** в веб-приложениях.
- **Хранение данных** в базах данных, которые используют JSON-подобные форматы для документов (например, MongoDB).
- **Создание тестовых данных** и макетов для разработки и тестирования приложений.

### Где открыть и работать с JSON

**Открыть JSON-файл** можно с помощью:

- **Текстовых редакторов** (например, «Блокнот»).
- **Редакторов кода** (Visual Studio Code, Sublime Text).
- **Онлайн-просмотрщиков** JSON (JSONViewer, JSON Editor Online).
- **Веб-браузеров** (Chrome, Firefox) — они автоматически форматируют данные JSON для удобства чтения.

---
#JSON