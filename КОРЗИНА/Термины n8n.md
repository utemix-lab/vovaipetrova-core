
## Webhook

Это способ для одной системы сообщить другой о событии. Входная точка (обычно URL), на которую отправляются данные при каком-то событии.

---

### 📦 Пример:

Ты хочешь, чтобы при публикации нового видео на YouTube, в Telegram ушёл пост:

1. YouTube не будет сам "звонить" в Telegram.
    
2. Но ты можешь создать Webhook в `n8n`, который будет "ждать событие".
    
3. Настроить сторонний сервис (или через RSS, или через YouTube API), чтобы **отправлял данные** на этот Webhook.
    
4. А `n8n`, получив сигнал — сразу выполнит нужный Workflow:
    
    - возьмёт заголовок видео,
        
    - сгенерирует пост,
        
    - отправит в Telegram и, скажем, на сайт.
        

---

### 🧠 Метафора:

Webhook — как дверной звонок. Пока не нажмут — n8n сидит и ждёт. Как только пришёл сигнал — запускается автоматическая последовательность.