# Быстрая настройка GitHub Copilot — проверка системы

## ✅ Результаты проверки (2026-01-04)

### 1. Ветка и рабочее дерево
```powershell
git branch --show-current
# Результат: main ✅

git status --porcelain
# Результат: только untracked файлы (prototype/page/*.html) — это нормально ✅
```

### 2. Версии инструментов
```powershell
node --version
# Результат: v24.11.0 ✅

npm --version
# Результат: 11.6.1 ✅

gh --version
# Результат: gh version 2.83.0 ✅
```

### 3. MCP серверы
```powershell
Test-Path .\mcp-server-notion.mjs
# Результат: True ✅

Test-Path .\mcp-server-openrouter.mjs
# Результат: True ✅
```

### 4. Файл .env
```powershell
if (Test-Path .env) {
  Select-String -Path .env -Pattern 'NOTION_API_KEY|OPENROUTER_API_KEY|GITHUB_TOKEN|GH_TOKEN' -SimpleMatch
}
# Результат: 
# - GITHUB_TOKEN ✅
# - NOTION_API_KEY ✅
```

### 5. Аутентификация gh
```powershell
gh auth status
# Результат: ✓ Logged in to github.com account utemix-lab ✅
```

## 🚀 Быстрая настройка MCP для Copilot

### Шаг 1: Установите зависимости (если еще не установлены)
```powershell
cd "R:\vovaipetrova-core"
npm install
```

### Шаг 2: Создайте конфигурацию MCP для VS Code/Copilot

**Путь к файлу конфигурации (Windows):**
```
%APPDATA%\Code\User\globalStorage\github.copilot\mcp.json
```

**Создайте файл с содержимым:**
```json
{
  "mcpServers": {
    "notion": {
      "command": "node",
      "args": [
        "R:\\vovaipetrova-core\\mcp-server-notion.mjs"
      ],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      }
    },
    "openrouter-file-access": {
      "command": "node",
      "args": [
        "R:\\vovaipetrova-core\\mcp-server-openrouter.mjs"
      ],
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
        "PROJECT_ROOT": "R:\\vovaipetrova-core"
      }
    }
  }
}
```

**Важно:**
- Путь `R:\\vovaipetrova-core` должен соответствовать вашему реальному пути
- Переменные `${NOTION_API_KEY}` и `${OPENROUTER_API_KEY}` будут подставлены из `.env` файла
- Если переменные не подставляются, можно указать токены напрямую (но не коммитьте их в репозиторий!)

### Шаг 3: Включите Agent Mode в VS Code

1. Откройте VS Code
2. Нажмите `Ctrl+,` (настройки)
3. Найдите "GitHub Copilot"
4. Найдите опцию **"Agent Mode"** или **"Enable MCP"**
5. Включите её
6. **Полностью перезапустите VS Code** (закройте и откройте заново)

### Шаг 4: Проверка работы

1. Откройте чат Copilot (`Ctrl+L` или через панель)
2. Попробуйте запрос:
   ```
   Найди в Notion страницы про "автоматизация"
   ```
3. Или:
   ```
   Прочитай файл docs/README.md
   ```

Если Copilot может использовать инструменты — всё работает! ✅

## 📚 Дополнительная документация

- **Подробная инструкция:** `docs/copilot-mcp-setup.md`
- **Быстрый старт Notion:** `COPILOT-NOTION-SETUP.md`
- **MCP Quickstart:** `MCP-QUICKSTART.md`

## 🔧 Troubleshooting

### Copilot не видит MCP сервер

1. **Проверьте версию VS Code:** должна быть ≥ 1.102
2. **Проверьте путь к конфигурации:**
   ```powershell
   Test-Path "$env:APPDATA\Code\User\globalStorage\github.copilot\mcp.json"
   ```
3. **Проверьте логи VS Code:** Help → Toggle Developer Tools → Console
4. **Перезапустите VS Code полностью**

### MCP сервер не запускается

Проверьте работу сервера вручную:
```powershell
cd "R:\vovaipetrova-core"
node mcp-server-notion.mjs
```

Должно показать:
```
MCP сервер Notion запущен
Notion API Base: https://api.notion.com/v1
API Key: ✅ установлен
```

### Agent Mode не включается

1. Проверьте подписку Copilot (может требовать Copilot Pro/Business)
2. Проверьте настройки организации (если корпоративный аккаунт)

## ✅ Все готово!

Система проверена и готова к работе. Следуйте шагам выше для настройки MCP в Copilot.

