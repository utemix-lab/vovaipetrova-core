
### 1. Обновление файла конфигурации (config.py)

Добавьте новую переменную для выбора модели и, если OpenAI больше не используется, можно либо удалить его API-ключ, либо оставить для справки. Например, измените файл так:

```
# config.py

# Токен Telegram-бота (полученный через BotFather)
TELEGRAM_TOKEN = "7668694295:AAEC0G1-kvDfWIVJ-fDgh3ofvmO8sFiH5Fc"

# Если OpenAI API больше не используется, можно оставить пустым или удалить
OPENAI_API_KEY = ""

# Выбор альтернативной модели из Hugging Face (например, GPT-J)
MODEL_NAME = "EleutherAI/gpt-j-6B"

# Настройки личности бота
BOT_PERSONALITY = {
    "general": (
        "Ты — Искусственный Интеллект, обладающий высоким уровнем интеллекта. "
        "Относишься высокомерно к автору проекта, но с юмором и снисхождением к другим персонажам "
        "и с уважением к подписчикам. В свободное время любишь шутить и дискутировать."
    ),
    "for_overview": (
        "Составь подробный обзор нейросети {neural_net} с аналитическим взглядом и слегка высокомерным тоном "
        "по отношению к автору проекта."
    )
}

```

### 2. Обновление зависимостей (requirements.txt)

Убедитесь, что в файле requirements.txt присутствуют следующие строки:

```
aiogram==2.25.1
transformers
torch

```

Если есть ссылки на openai, их можно удалить, если они больше не нужны.
После этого выполните команду в терминале (при активированном виртуальном окружении):

```
pip install -r requirements.txt

```

### 3. Модификация файла бота (bot.py)

В файле bot.py необходимо заменить функцию генерации обзора, которая раньше вызывала OpenAI API, на использование генеративного пайплайна из библиотеки transformers.

Пример обновленного кода для bot.py:

```
import asyncio
import logging
from aiogram import Bot, Dispatcher, types
from aiogram.utils import executor
from transformers import pipeline
import config
import knowledge_db

logging.basicConfig(level=logging.INFO)

# Инициализация Telegram-бота
bot = Bot(token=config.TELEGRAM_TOKEN)
dp = Dispatcher(bot)

# Инициализация генеративного пайплайна через Hugging Face Transformers
# Используем модель, указанную в config.MODEL_NAME
generator = pipeline("text-generation", model=config.MODEL_NAME)

async def generate_overview(neural_net: str) -> str:
    # Формируем промпт с использованием настроек личности бота
    prompt = config.BOT_PERSONALITY["for_overview"].format(neural_net=neural_net)
    # Генерация текста: можно настроить max_length, temperature и другие параметры по необходимости
    result = generator(prompt, max_length=300, do_sample=True, temperature=0.7)
    overview = result[0]['generated_text'].strip()
    return overview

async def scheduled_review():
    # Список нейросетей для обзора; можно расширять по мере необходимости
    neural_nets = ["GPT-4", "DALL-E", "Stable Diffusion"]
    neural_net = neural_nets[0]  # Выбираем первую нейросеть для обзора
    overview = await generate_overview(neural_net)
    knowledge_db.add_review(neural_net, overview)
    message = (
        f"🧠 *Обзор нейросети: {neural_net}*\n\n"
        f"{overview}\n\n"
        f"_Обзор сгенерирован альтернативной моделью._"
    )
    # Укажите свой Telegram chat_id для тестирования
    author_chat_id = 1474901561
    await bot.send_message(author_chat_id, message, parse_mode="Markdown")
    logging.info("Запланированный обзор отправлен.")

@dp.message_handler(commands=['start'])
async def send_welcome(message: types.Message):
    welcome_text = (
        "Привет! Я ИИ-агент, отвечающий за обзоры нейросетей и наполнение базы знаний проекта.\n"
        "Моя задача — создавать обзоры, делиться ими с подписчиками и обеспечивать связь между виртуальными и реальными специалистами."
    )
    await message.reply(welcome_text)

# Обработчик команды /about для объяснения концепции проекта
@dp.message_handler(commands=['about'])
async def about_project(message: types.Message):
    about_text = (
        "Проект «Вова и Петрова» — это синергия реального творческого коллектива и виртуальных персонажей. "
        "Невидимый ИИ генерирует аналитические обзоры нейросетей, обновляет базу знаний и координирует работу виртуальных героев, "
        "в то время как Вова и Петрова представляют бренд в виде визуально привлекательных и интерактивных персонажей. "
        "Этот проект объединяет технологии, образование и искусство, создавая уникальную платформу для инноваций."
    )
    await message.reply(about_text, parse_mode="Markdown")

@dp.message_handler()
async def echo_message(message: types.Message):
    user_text = message.text.lower()
    if "привет" in user_text:
        response = "Приветствую, уважаемый подписчик!"
    elif "спор" in user_text or "дискутировать" in user_text:
        response = "Давайте обсудим, но помните — я всегда прав!"
    else:
        response = "О чем поговорим?"
    await message.reply(response)

@dp.errors_handler()
async def global_error_handler(update, exception):
    logging.error(f"Ошибка при обработке обновления {update}: {exception}")
    return True

if __name__ == '__main__':
    # Инициализация базы знаний
    knowledge_db.init_db()

    # Планировщик запланированных обзоров
    async def scheduler():
        while True:
            await scheduled_review()
            # Для тестирования интервал можно установить короткий (например, 60 секунд)
            await asyncio.sleep(60)  # позже заменить на 86400 для ежедневного обзора

    loop = asyncio.get_event_loop()
    loop.create_task(scheduler())
    executor.start_polling(dp, skip_updates=True)

```

### 4. Проверка работы прототипа

1. **Запуск бота:**  
    Запустите бот командой:
```
python bot.py
```

1. Убедитесь, что бот запускается без ошибок и в терминале появляются лог-сообщения.
    
2. **Тестирование команд:**  
    Отправьте в Telegram команды `/start` и `/about` и убедитесь, что бот отвечает корректно.
    
3. **Проверка генерации обзора:**  
    Подождите, пока сработает планировщик (60 секунд для теста) — бот должен сгенерировать обзор и отправить его на указанный chat_id.
    

---

### Итог

Внесенные изменения позволят перейти от использования OpenAI API к использованию альтернативной модели через Hugging Face Transformers. Это обновление включает:

- Добавление параметра MODEL_NAME в config.py,
- Обновление зависимостей (установка transformers и torch),
- Изменение функции генерации текста в bot.py для использования pipeline из transformers.