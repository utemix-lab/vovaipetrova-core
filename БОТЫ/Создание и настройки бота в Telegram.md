
### Создание виртуального окружения:

```
python -m venv venv
```

### Активация виртуального окружения на Windows:
Префикс venv, присутствующий в дальнейшем означает пребывание в виртуальном окружении.

```
venv\Scripts\activate
```

### Установка зависимостей из файла requirements.txt:

```
pip install -r requirements.txt
```

### Установка библиотеки huggingface_hub:

Если модель скачивается с [[27_Hugging Face]].

```
   pip install huggingface_hub
```

### Запуск утилиты huggingface-cli (Лучше без VPN):

Эта утилита позволяет управлять моделями, загружать их с [[Hugging Face Hub]] и выполнять другие операции через командную строку. Она поддерживает такие функции, как возобновление прерванных загрузок , что может быть полезно при скачивании больших файлов. Запуск утилиты huggingface-cli для скачивания определенной модели, например:

```
huggingface-cli download EleutherAI/gpt-j-6B
```

Подробнее о командах и возможностях утилиты можно узнать в документации по ссылке: [Hugging Face CLI](https://huggingface.co/docs/huggingface_hub/guides/cli)

### Возможные ошибки, причины и пути решения: 

**ChunkedEncodingError**

- Возможно, нестабильное Интернет-соединение. Попробуйте подключиться к более стабильной сети или перезапустить загрузку. Такие большие файлы чувствительны к временным перебоям в сети.
- Если время ожидания и стабильность соединения являются проблемой, можно рассмотреть использование модели меньшего размера (например, GPT‑Neo 2.7B), которая скачивается быстрее и занимает меньше места.
- Просто перезапустите запуск бота, чтобы библиотека Transformers попыталась снова скачать модель. Иногда повторные попытки могут сработать, если соединение временно улучшилось.

```
python bot.py
```

**SSLError** 

- Разрыв SSL-соединения. Возможно, проблемы с локальной средой SSL/TLS.
- Убедитесь, что у вас установлены актуальные версии Python и библиотеки OpenSSL, так как устаревшие версии могут не поддерживать современные протоколы TLS.
- Можно временно отключить проверку SSL, добавив параметр verify=False в запросы. Но это снижает безопасность и должно использоваться только для тестирования, если нет других вариантов.
- Проверка версии OpenSSL использующейся в вашей среде Python.
- В командной строке Windows команды Python-кода не выполняются напрямую. Чтобы проверить версию OpenSSL, откройте интерактивную оболочку Python, введя в терминале:

```
python
```

- В открывшейся оболочке введите:

```
import ssl
```

```
print(ssl.OPENSSL_VERSION)
```

- Вы увидите строку, например: OpenSSL 1.1.1k 25 Mar 2021
- Чтобы выйти из интерактивной оболочки, наберите:

```
exit()
```

- Или на Windows можно нажать `Ctrl+Z` и затем Enter.

- Если модель уже была успешно загружена с помощью huggingface-cli и сохранена в локальном кэше, то повторно запускать huggingface-cli не требуется. Библиотека Transformers автоматически использует локальные файлы модели из кэша, если они доступны.
- То есть команда python bot.py должна загрузить модель из локального хранилища и запустить бота. huggingface-cli полезен именно для скачивания модели, если загрузка прерывается или нужно обновить модель. На данном этапе, если модель скачана, huggingface-cli не понадобится для работы бота.
- Если бот не запускается или возникают ошибки, проверь, что модель действительно находится в стандартном кэше Hugging Face (обычно это папка C:\Users\Администратор\.cache,  или соответствующий путь в Windows) и что переменная `MODEL_NAME` в `config.py` указывает на правильное название модели.
- Отключить проверку SSL в библиотеке requests, чтобы можно было дозагрузить модель, если возникает ошибка SSLError. Обратите внимание, что отключение проверки SSL представляет угрозу безопасности и должно использоваться только для тестирования, а не в продакшене.
- Можно «замонки-патчить» метод request у requests.Session так, чтобы он всегда передавал параметр `verify=False`. Например, добавьте следующий код в начало вашего скрипта (до загрузки модели):

```
import requests
import urllib3

# Отключаем предупреждения о невалидных сертификатах
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

# Сохраняем оригинальный метод request
_original_request = requests.Session.request

# Определяем новую функцию, которая всегда передаёт verify=False
def _new_request(self, method, url, *args, **kwargs):
    kwargs['verify'] = False
    return _original_request(self, method, url, *args, **kwargs)

# Подменяем метод в requests.Session
requests.Session.request = _new_request

```
- Добавьте этот фрагмент кода в начало вашего основного скрипта (например, в файле `bot.py` или в другом подходящем месте) **до** импорта и использования генеративного пайплайна из transformers.

- После этого все запросы, выполняемые через requests, будут игнорировать проверку SSL-сертификата. Таким образом, если проблема была связана с SSL-проверкой, загрузка модели может продолжиться.

- **Важно:**

- Используйте этот способ только для тестирования или в закрытой среде, так как отключение проверки сертификатов снижает безопасность соединения.
- После решения проблемы рекомендуется убрать этот код и обеспечить нормальную проверку SSL.


### Работа с ботом

-  Запуск бота:

```
python bot.py
```

-  Получение обновлений (getUpdates) через API Telegram (для поиска chat_id): Откройте браузер и введите (заменив `<YourBotToken>` на реальный токен):

```
https://api.telegram.org/bot<YourBotToken>/getUpdates
```

- Или через терминал:
   
```
curl https://api.telegram.org/bot<YourBotToken>/getUpdates
```

- Команда для отключения режима приватности (через BotFather): Отправьте в [[BotFather]]:

```
/setprivacy
```

   и выберите нужного бота, затем «Disable» для отключения приватности (если требуется).

### Дополнительные полезные команды

- Просмотр списка установленных пакетов (для проверки):

```
pip freeze
```

- Просмотр точного списка ваших ботов через BotFather: Отправьте в BotFather команду:

```
/mybots
```

### Добавление бота в Telegram-канал в качестве администратора

#### *Имя бота*

Его username и/или отображаемое имя обычно задаётся через BotFather при создании бота, и Telegram хранит эту информацию на своих серверах. В коде проекта, как правило, используется токен для аутентификации, а имя бота можно не указывать, так как оно не требуется для работы API.
#### *Чтобы бот мог публиковать сообщения в канале.*
Его нужно добавить в этот канал с правами администратора:

1. **Откройте канал в Telegram.**  
    Зайдите в приложение Telegram и выберите нужный канал, где бот должен публиковать сообщения.
    
2. **Перейдите в настройки канала.**  
    Нажмите на название канала, затем выберите «Управление каналом» (или «Настройки», в зависимости от версии приложения).
    
3. **Добавление администратора:**
    
    - Найдите раздел «Администраторы» или «Управление администраторами».
    - Нажмите кнопку «Добавить администратора».
    - Введите имя или username вашего бота (например, если у него имя @YourBotUsername).
    - Выберите вашего бота из списка.
4. **Настройка прав:**  
    Убедитесь, что бот получает права на публикацию сообщений. Обычно достаточно разрешить отправку сообщений и, возможно, управление сообщениями ==(галочка "Delete mesages")==. Сохраните изменения.
    

После этого бот станет администратором канала и сможет публиковать сообщения.


### Где находится бот и как посмотреть его данные

В [[BotFather]] нет отдельной настройки, которая прямо разрешает отправку сообщений, поскольку бот по умолчанию может отправлять сообщения. Если бот не отправляет сообщения, причина, как правило, не в настройках BotFather, а в следующем:

- **Для групп:**  
 Если бот добавлен в группу, он может иметь включённый режим приватности (privacy mode). Чтобы бот мог читать все сообщения в группе (а это может быть важно для некоторых сценариев), его нужно отключить режим приватности командой `/setprivacy` в BotFather. Но это не ограничивает отправку сообщений — бот отправляет сообщения по умолчанию.

- **Для каналов:**  
 Чтобы бот мог публиковать сообщения в канале, его нужно добавить в этот канал как администратора. Это делается не через BotFather, а через настройки самого канала в Telegram. После добавления бота с правами администратора он сможет отправлять сообщения в канал.

### Если бот не отправляет сообщения

Убедитесь, что:

- Вы используете правильный токен и бот успешно авторизован;
- Для групп — при необходимости отключен режим приватности, если требуется, чтобы бот получал все сообщения (это делается командой `/setprivacy` в BotFather, выбрав «Disable»);
- Для каналов — бот добавлен в канал с соответствующими правами администратора (право на публикацию сообщений).

Таким образом, настройка отправки сообщений в BotFather не требуется, а необходимо корректно настроить интеграцию бота с нужной группой или каналом через их собственные настройки.

### Описание бота для пользователей в Telegram

Как добавить описание?

Тестовое: 
"Этот бот может с вами поговорить о кораблях, башмаках, сургуче и даже о королях и капусте."

### Чтобы бот публиковал сообщения в нужную группу или канал

#### *Есть несколько способов получить chat_id канала.*

Один из самых надёжных методов – использовать метод getUpdates API Telegram. Вот как это сделать:

- **Добавьте бота в канал как администратора.**  
  Убедитесь, что ваш бот добавлен в нужный канал и имеет права публиковать сообщения.

- **Отправьте сообщение в канал.**  
  Например, опубликуйте сообщение от имени бота (или вручную через админа). Это нужно для того, чтобы в обновлениях (updates) появился объект с информацией о сообщении из канала.
 
-  **Вызовите метод getUpdates.**  
   Откройте браузер и в адресной строке введите URL следующего вида, заменив `<YourBotToken>` на реальный токен вашего бота: https://api.telegram.org/bot<YourBotToken>/getUpdates

- **Проанализируйте JSON-ответ.**  
  Вы увидите JSON-объект с обновлениями. Найдите тот объект, который соответствует сообщению, отправленному в канал. В этом объекте будет поле `chat`, содержащее информацию о канале.  
  Пример фрагмента:
```
  {
  "update_id": 123456789,
  "channel_post": {
    "message_id": 42,
    "chat": {
      "id": -1001234567890,
      "title": "Название вашего канала",
      "type": "channel"
    },
    "date": 1680000000,
    "text": "Пример сообщения"
  }
}

```
  
#### *Определить переменную для ID группы в файле конфигурации.*  
Можно добавить в файл `config.py` новую переменную, например:
```
#Config.py 
   
# Токен Telegram-бота
TELEGRAM_TOKEN = "7668694295:AAEC0G1-kvDfWIVJ-fDgh3ofvmO8sFiH5Fc"
   
# Имя бота (для удобства) BOT_NAME = "@VovaIPetrovBot"
   
# API-ключ для OpenAI (если используется, или можно оставить пустым) OPENAI_API_KEY = "" 

# Выбор альтернативной модели (например, GPT-J) MODEL_NAME = "EleutherAI/gpt-j-6B"

# ID группы или канала для публикаций
# Например, для канала обычно используется отрицательное число, как -1001234567890
GROUP_CHAT_ID = -1001234567890

# Настройки личности бота
BOT_PERSONALITY = { 
"general": ( 
"Ты — Искусственный Интеллект, обладающий высоким уровнем интеллекта. " "Относишься высокомерно к автору проекта, но с юмором и снисхождением к другим персонажам "
"и с уважением к подписчикам. В свободное время любишь шутить и дискутировать."
), 
"for_overview": ( 
"Составь подробный обзор нейросети {neural_net} с аналитическим взглядом и слегка высокомерным тоном " "по отношению к автору проекта." 
) 
}
```
Здесь значение `-1001234567890` – это и есть chat_id канала (обратите внимание, что для каналов он обычно отрицательный). Используйте найденный chat_id, копируйте его и вставьте в коде вашего бота для отправки сообщений в канал.

### Проверка корректной установки зависимостей и настроек файлов

- Проверьте, что в файле [[config.py]] указаны все необходимые параметры ([[токен]], [[имя бота]], [[имя модели]], [[GROUP_CHAT_ID]], если нужно).
- Убедитесь, что файл [[requirements.txt]] содержит все нужные библиотеки ([[aiogram]], [[transformers]], [[torch]] и т.д.), и что они установлены командой:

  [[pip install -r requirements.txt]]

### Запуск бота

В терминале, находясь в корневой папке проекта, выполните команду:

```
python bot.py
```

Если всё настроено правильно, бот начнет загружать [[модель]] (уже скачанную локально) и инициализируется.

## ⚙️ **Что останется на компьютере после запуска?**

1. **Вес самой модели**:
    
    - **GPT-J-6B** весит около **20 ГБ** в сжатом виде и примерно **22-25 ГБ** в распакованном.
    - Учитывай дополнительные файлы: токенизаторы, конфиги, кэш. В сумме может доходить до **30 ГБ**.
2. **Логи работы**:  
    Если агент будет долго работать, появятся логи: диалоги, команды, ошибки. Для активного использования можно выделить ещё **2-5 ГБ**.
    
3. **Дополнительные компоненты**:
    
    - Если планируешь подключать **интеграцию с Telegram** или другими сервисами, появятся зависимости и конфигурации (ещё **1-2 ГБ**).
    - Возможен кэш генерации, особенно если агент будет генерировать много текста или использовать мультимодальные модели.

**🎯 Рекомендуемый запас:** **40-50 ГБ**. Это с учётом запаса на логи и дополнительные настройки.

---

## 🔋 **Нужен ли включённый компьютер для работы бота?**

Да, если мы запускаем бота **локально** на твоём ПК, он будет работать **только при включённом компьютере**.  
Причина проста:

- Модель работает в твоей системе.
- Агент обрабатывает запросы, пока у него есть доступ к вычислительным ресурсам.

---

## ☁️ **Варианты для работы без включённого ПК**

Если хочется, чтобы бот работал **24/7**, есть несколько вариантов:

### 1️⃣ **Виртуальный сервер (VPS)**

- Арендуешь сервер, устанавливаешь туда модель и запускаешь.
- **Минусы:** нужен мощный сервер, стоимость от **20-100$ в месяц** при использовании больших моделей.
- Провайдеры: **Hetzner**, **Contabo**, **AWS**, **Google Cloud**.

### 2️⃣ **Облачные платформы**

- **Hugging Face Spaces**, **RunPod**, **Replicate** – позволяют запускать модели в облаке.
- Можно подключить к Telegram через API.
- Платишь за фактическое время работы.

### 3️⃣ **Telegram Bot + API**

- Агент может работать на твоём компьютере, но через **бот Telegram**.
- Ты можешь запускать его вручную, а остальное время он будет **просто ждать подключения**.

---

## 🚀 **Оптимальное решение на старте**

На начальном этапе достаточно запускать бота **локально**.

- Проверишь, как он работает.
- Отладишь взаимодействие с пользователями.
- Поймёшь, нужны ли дополнительные мощности.

Могу помочь позже с оптимизацией работы, чтобы при включении компьютера бот запускался автоматически.

Как тебе такой план? 😎

### Тестирование бота
Откройте Telegram и отправьте боту команду `/start` в личном чате, чтобы убедиться, что он отвечает приветственным сообщением.
Отправьте команду `/about` (если такой обработчик реализован), чтобы получить описание концепции проекта.
Подождите, пока сработает функция `scheduled_review()`. Если вы установили тестовый интервал (например, 60 секунд), бот должен сгенерировать обзор и отправить сообщение в указанный чат (проверьте, что GROUP_CHAT_ID корректен).
Проверить логи и исправить ошибки:
Следите за выводом в терминале. Если появляются ошибки или предупреждения, их нужно устранить (например, настройки API, проблемы с генерацией текста и т.д.).
Если всё работает корректно, значит, прототип бота готов к дальнейшей доработке и интеграции в общий проект

#### Улучшение обработки сообщений и логирования
Чтобы бот работал стабильно, можно добавить дополнительные обработчики ошибок и расширить логирование. Например, добавить глобальный обработчик ошибок:
```
@dp.errors_handler()
async def global_error_handler(update, exception):
    logging.error(f"Ошибка при обработке обновления {update}: {exception}")
    return True  # Чтобы исключения не пробрасывались дальше

```
####  План дальнейших доработок


После того как базовая функциональность будет протестирована, можно переходить к следующим этапам:

- **Интеграция с базой знаний:** Разработка системы, где бот сможет динамически извлекать информацию из базы знаний (например, из файлов Obsidian или из SQLite) для более гибких ответов.
- **Расширение диалоговой логики:** Добавление обработчиков для других ключевых запросов (например, `/help`, `/portfolio`).
- **Разделение функций:** При необходимости, можно разделить функции генерации контента на несколько специализированных модулей (например, один для генерации обзоров, другой — для образовательных материалов).

