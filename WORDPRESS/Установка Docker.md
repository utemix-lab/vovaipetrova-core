
## 🔧 Шаг 1. Установи Docker Desktop

### 📥 Скачай Docker Desktop отсюда:

👉 https://www.docker.com/products/docker-desktop

Выбери версию **для Windows** (он сам определит, что у тебя Pro).

### ⚙️ Установка:

- Запусти `.exe` как **администратор** (ПКМ → "Запуск от имени администратора")
    
- Соглашайся со всеми пунктами, **разреши использовать WSL2** (это нужно — не страшно)
    
- После установки перезагрузи компьютер
    

### ✅ Проверка:

- Запусти Docker Desktop
    
- В трее (рядом с часами) появится иконка 🐳
    
- Открой PowerShell и введи:

```
docker version

```

Если показывает версию — всё работает.

### 💡 Что значит "разрешать использование компонентов Windows"?

Когда ты устанавливаешь **Docker Desktop на Windows**, он предлагает включить или установить системные компоненты, которые **необходимы для его работы**.

Обычно это:

---

### ✅ Обязательные компоненты:

1. **WSL 2 (Windows Subsystem for Linux v2)** — это специальная подсистема, которая позволяет Docker работать как бы "на базе Linux" прямо внутри Windows.
    
    - Docker использует WSL 2 как основу, чтобы запускать контейнеры.
        
    - Если у тебя ещё не установлен WSL 2, Docker предложит его поставить — **разрешай!**
        
2. **Hyper-V** _(для некоторых режимов)_ — виртуализация от Microsoft. Иногда Docker использует его вместо WSL, особенно в Enterprise-сценариях.
    
    - Если ты не используешь виртуалки отдельно — **разреши установку, если предложит**.
        
3. **Виртуализация в BIOS** — должна быть включена. Если выключена — Docker не сможет работать. Проверим при ошибке.
    

---

### 📌 Что делать?

👉 **Разрешай установку всех компонентов, которые предложит Docker.** Это безопасно и необходимо для его работы.

Если что-то не установится — я помогу вручную.

---

### 🚀 Примерный порядок:

- Установка Docker →
    
- Появится окно: "Разрешить установить WSL 2 / Hyper-V / доп. компоненты?" →  
    ✅ **Нажимай "Да" / "Разрешить" / "ОК"**

## Запуск Docker

### 1. Создай новую папку для проекта

Например:

- `C:\wordpress-test`  
    (можешь любую, но **без кириллицы** в пути!)
    

---

### 2. Создай в этой папке файл с именем `docker-compose.yml`

Скопируй в него вот это:

```
version: '3.1'

services:
  wordpress:
    image: wordpress
    restart: always
    ports:
      - 8080:80
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: user
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: wordpress
    volumes:
      - ./wp_data:/var/www/html

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: wordpress
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: rootpass
    volumes:
      - ./db_data:/var/lib/mysql

```

### 3. Запусти PowerShell от имени администратора

Перейди в созданную папку, запусти команду:

```
cd C:\Wordpress-test
docker-compose up -d

```

### 4. Открой в браузере

Перейди по ссылке:  
**[http://localhost:8080](http://localhost:8080)**

Появится окно установки WordPress 🎉  
Выбери язык, задай логин и пароль — и всё готово!

## 🚀 Что делать дальше:

### 1. **Открыть сайт WordPress**

- Перейди в браузере по адресу:  
    👉 `http://localhost:8080`
    
- Ты увидишь страницу установки WordPress (выбор языка, ввод названия сайта и т.д.).
    

---

### 2. **Завершить установку WordPress**

- Выбери язык (например, русский).
    
- Заполни поля:
    
    - Название сайта
        
    - Имя пользователя (например, `admin`)
        
    - Пароль
        
    - Email
        
- Нажми **Установить WordPress**.
    

---

### 3. **Вход в админку**

После установки зайди в админку:  
👉 `http://localhost:8080/wp-admin`  
Введи логин и пароль, которые только что создал.

---

### 🧠 Дополнительно (по желанию)

### ✅ Как остановить сервер:

```
docker-compose down

```

### ✅ Как снова запустить:

```
docker-compose up -d

```

### ✅ Где хранятся данные:

- Твои файлы WordPress — в папке `wp_data`
    
- База данных — в `db_data`
    

Они лежат рядом с `docker-compose.yml`.

---
#Docker